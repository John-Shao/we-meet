# 阶段 3 第十九批（累计第 39 批）：纪要导出预览与持久化请求

新增纪要导出预览、请求和个人回执查询接口。用户选择明确的 AI 版本或人工修订版本及导出语言，预览确认后冻结文档标题、正文、所有者和 Docs 服务地址。凭证不进入任务记录。

- 预览哈希参与提交校验；标题或内容变化必须重新审阅。
- 相同用户、记录、版本和语言的多浏览器请求归并为同一个导出；请求键重复使用但内容不同返回冲突。
- 仅当前记录所有者或线上会议管理者可发起，摘要共享读者无导出权限。查询只返回本人回执，不暴露正文或服务凭证。
- 模型与人工文字作为普通文本转义，保留来源时间及版本标识；待办仍是待确认内容，不自动创建任务。
- 开关 `MEETING_SUMMARY_EXPORT_ENABLED` 默认关闭。后台交付和页面入口尚未接通，本批不能单独开启业务。

验证：9 项服务/API 测试通过，包括并发去重、权限撤销、版本冻结和预览变更；迁移 0166 已在隔离数据库验证；迁移一致性、Ruff 和差异检查通过。没有调用真实 Docs 或发送消息。
