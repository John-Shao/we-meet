# 阶段 3 第二十四批（累计第 44 批）：完成事件与私人通知账本

最终版本化纪要发布时，在同一事务中记录一次完成事件和固定接收者名单。新增通知账本与私人状态接口，通知处理状态独立于纪要生成状态。

- 独立录音只选择创建者；线上选择当前会议所有者、真实记录发起人及纪要请求人，去重且必须仍有纪要读取权限。系统没有独立的历史主持人字段，因此不把会议所有者冒称为实际主持人。共享读者、未知说话人不会自动成为接收者。
- 接收者集合包含空集合也只冻结一次，后加所有者不补收旧通知。仅新发布的最终版本产生事件，不扫描旧版本或为实时/速记刷屏。
- 冻结标题、来源、发生时间、版本时间和接收者身份；消息不嵌入逐字稿或 AI 正文。服务配置缺失时仍保留待处理任务，文档与 IM 地址在首次交付准备时绑定，绑定后不改写。
- 保留现有旧纪要分发策略。状态接口明确告知新版本默认策略，管理者可查看未来接收者候选；每个人只看到自己的投递状态，通知不授予材料权限。
- 复用已有会议助手身份承载“智能纪要已生成”卡片，不再创建第二个同类机器人。

验证：12 项通知场景及 16 项版本化纪要测试通过；9 项独立录音纪要回归通过。0167 在隔离数据库迁移成功，迁移一致性、Django 系统检查、Ruff 和差异检查通过。尚未接发送 Worker；开关 `MEETING_SUMMARY_NOTIFICATIONS_ENABLED` 默认关闭，继续保持关闭到后续交付与页面完成。

下一批补齐首次交付准备、回执查询、单次发送、未知结果恢复和明确重试；卡片深链的版本定位也需接入 Web。没有发送真实消息。
