# 会议 AI 阶段 2：第 1–9 批执行记录

整理日期：2026-09-22。

收录会议转写、原文快照、纪要与在线采集控制的执行记录。

本文件按原批次 / 日期合并，保留原文、验证记录和当时状态，仅调整标题层级与文档链接。正文中的“本批”“下一批”“已通过”均为历史记录，不代表本次重新验证或当前部署状态。原文件名用于追溯；阶段内批次与累计批次沿用原编号。

[返回方案目录](README.md)

## 目录

- [会议 AI 阶段 2：第一批（累计第七批）](#phase2-batch1)
- [会议 AI 阶段 2：第二批（累计第八批）](#phase2-batch2)
- [会议 AI 阶段 2：第三批（累计第九批）](#phase2-batch3)
- [会议 AI 阶段 2：第四批（累计第十批）](#phase2-batch4)
- [会议 AI 阶段 2：第五批（累计第十一批）](#phase2-batch5)
- [会议 AI 阶段 2：第六批（累计第十二批）](#phase2-batch6)
- [阶段 2 第七批（累计第十三批）：在线采集控制服务](#phase2-batch7)
- [阶段 2 第八批（累计第十四批）：Agent 执行与会中采集按钮](#phase2-batch8)
- [阶段 2 第九批（累计第十五批）：记录结束边界与参会提示](#phase2-batch9)

---

<a id="phase2-batch1"></a>

来源：`meeting-ai-phase2-batch1-2026-09-13.md`。

## 会议 AI 阶段 2：第一批（累计第七批）

日期：2026-09-13。基线：`d1bb3a1f`。实现 C01 的可配置 Qwen 实时 ASR 适配和 Agent 对接；阶段 1 待部署验收项不因此自动完成。部署和实际业务音频测试由用户负责。

### 实现

- `STT_PROVIDER=qwen` 走 QwenSTT；保留原 deepgram/kyutai/doubao 配置回退。模型默认候选 `qwen-audio-3.0-asr-flash-streaming`，用 QWEN_ASR_MODEL 覆盖。业务质量仍待测试，不宣称最终选型质量验收已完成。
- 每个参会者独立 STT 流和供应商 task_id。只对非空 final 写正式原文；SDK 简化字幕事件继续可用，但不再次落库。原文 ingest_id 由供应商任务 UUID 和 sentence_id 确定，HTTP 重试保留；重复 final 去重，内容变更冲突报错。
- 保存真实返回的句子 begin/end 偏移，转换为入站音频观察时间；不再把模型返回时刻作为句子时间。PCM 到达用单调时钟记录间隔，超过 250 ms 的明显输入中断保留在时间映射中，null 结束偏移保持 null。时间精度属于 Agent 入站观察，尚非设备采样时钟或精确媒体定位证明。
- 真实 LiveKit SpeechStream 负责重采样至 16 kHz 单声道 PCM。待处理音频最多 10 秒，溢出或非单声道显式失败；不静默丢帧。关闭先 end_input 排空，再等待 finish-task 的 task-finished，超时标记文字送达不完整。
- 没有 PCM 不连接供应商；5 秒无新输入时正常收尾当前任务，有真实输入后创建新任务，避免静音麦克风空连接超时。不伪造静音填充。累计源偏移跨任务保留；每个新任务有独立 ingest 身份。
- 禁止 SDK 自动重放部分消费过的源音频；连接错误以 sanitized 错误上报，并调用现有 writer.mark_incomplete。暂未实现失败任务的音频持久重放。暂停/空闲正常换任务与网络错误不是同一种结果。
- WSS 仅允许已配置 workspace 和北京/新加坡地域；拒绝 HTTP 重定向，密钥不出现在 repr、异常正文或日志。provider/model/凭据配置在每个参会者 STT 实例创建时固定。

协议按官方 [WebSocket API](https://www.alibabacloud.com/help/en/model-studio/fun-asr-realtime-websocket-api)、[客户端事件](https://help.aliyun.com/zh/model-studio/fun-asr-client-events)、[服务端事件](https://help.aliyun.com/en/model-studio/fun-asr-server-events) 核对：等 task-started 才送音频，发 finish-task 后继续接尾句，task-finished 才是该任务正常结束。这里的任务结束仍不等于整场会议音频覆盖完整。

### 部署配置（由用户执行）

Agent 环境设置 STT_PROVIDER=qwen、ENABLE_SILERO_VAD=False、DASHSCOPE_API_KEY、DASHSCOPE_WORKSPACE_ID、QWEN_ASR_REGION=cn-beijing、QWEN_ASR_MODEL=qwen-audio-3.0-asr-flash-streaming。密钥使用现有 Secret 管理方式，不写入仓库。参考 `env.d/development/multi_user_transcriber.dist`；该文件只是示例，未改实际运行环境。

正式原文送达账本还要求 Agent 的 AGENT_TRANSCRIPT_DELIVERY_ENABLED=true 以及后端 MEETING_RECORDS_ENABLED、MEETING_TRANSCRIPT_DELIVERY_ENABLED；Agent 原有 AGENT_BACKEND_API_URL、AGENT_INTERNAL_API_TOKEN 仍需配置。一个会议只启动一个正式原文 Agent，不同时运行新旧 provider 写入同一来源。

websockets 最低版本提高至 15.0，以使用明确禁用代理的连接参数；uv.lock 保持既有 16.0，uv 0.10.9 重新解析并通过 lock --check。没有升级其他依赖。

建议实测：开始时静音后开麦、长静音恢复、多人交替、结束时最后一句、网络异常、关闭重入和中英混合。检查准确会议场次、原文起止时间、重复写入与不完整状态。旧翻译配置仍使用原服务，这批没有切换语音翻译模型或自动发送消息。

### 检查与走查

23 项 Agent 单元/SDK 集成测试通过（原 13 项 + 新增 10 项），覆盖协议握手、尾句、重复/冲突 final、错误脱敏、task 范围、超时/取消、输入时间间隔、真实 48 kHz→16 kHz 重采样、重复关闭、静音恢复、SDK 字幕去重与时间落库。走查/测试修复了“任务刚结束时输入已关闭但队列仍有音频”的漏读竞态：现在确认队列为空才退出。

新增插件与测试 Ruff 通过；transcriber 除原有两处 PLC0415 条件 import 外通过，新增源事件辅助函数显式标注参数数量例外。没有后端/页面改动，不重复无关后端/前端构建。本批没有真实模型收费调用、部署或多端实测；阶段 0 连通记录不能代替新适配的上线质量验收。

下一批把供应商任务收尾观察和错误原因保存到后端，供源详情与纪要读取，不将 task-finished 升格为整场完整性证明。随后继续阶段 2 的实时/速记/完整纪要和页面，最后统一做技术评审与代码走查，修复发现的问题再交由用户部署测试。

---

<a id="phase2-batch2"></a>

来源：`meeting-ai-phase2-batch2-2026-09-13.md`。

## 会议 AI 阶段 2：第二批（累计第八批）

日期：2026-09-13。基线：`8771dec6`。本批保存 Qwen ASR 的源观察结果，并贯通原文送达、不可变快照和纪要阅读。

### 实现与边界

- Agent 分别统计已观察流、供应商任务、输入采样数、final 句数、计费秒数和有限错误码。任务身份去重，冲突、溢出及未收尾流显式记为异常；不保存音频正文、密钥或供应商异常原文。
- writer 结束时冻结终态请求，HTTP 重试复用相同状态与观察报告。晚到事件不能修改已经提交的报告；有异常或未结束任务时不声明完整送达。
- 后端对报告进行严格校验，报告与送达终态一起保存。终态重放必须完全相同；失败报告不能以 complete 封口。已有无报告的 Agent 保持兼容。
- 原文快照包含报告并参与指纹；已生成纪要读取当时的观察结果。新增 source-status 接口仅允许有纪要阅读权限的用户访问准确记录的场次，使用游标分页和 private/no-store 响应。
- 页面分别呈现原文送达状态、已观察音频的 ASR 收尾状态、整场音频覆盖状态。即使供应商 task-finished 全部收齐，整场覆盖仍为未验证；没有输入任务时显示“未观察到音频输入”。

### 检查

- Agent 27 项测试通过，包含真实 SDK 重采样/关闭时的观察回调，以及报告重放、冲突、边界和失败终态。
- 后端选定范围 139 项：第一次 138 通过，新增测试一项因只 mock chat 而触发部分测试环境的真实客户端构造失败；改为与现有测试一致地 mock 整个客户端后，本批全部 12 项通过。没有修改业务逻辑来绕开该测试。
- 前端 13 项测试通过，新增断言确认“ASR 尾段收齐”不会移除整场覆盖未验证提示；TypeScript、ESLint、Prettier 通过。
- 迁移 0153 在隔离数据库成功执行，迁移漂移检查无变化，Django check 通过。相关 Python Ruff 和 git diff --check 通过；models.py 原有两处 DJ012 未扩大处理。

### 部署与后续

由用户先部署后端并执行 0153，再更新 Agent，避免旧后端忽略新观察字段。沿用上一批开关；未操作实际部署、真实收费模型调用或消息推送。

本批不建立设备音频覆盖证明，也不自动恢复失败任务。下一批继续 C03：分阶段纪要、水位与并发来源变化处理；保留引用、权限和不可变版本约束。整体完成后统一技术评审与代码走查。

---

<a id="phase2-batch3"></a>

来源：`meeting-ai-phase2-batch3-2026-09-13.md`。

## 会议 AI 阶段 2：第三批（累计第九批）

日期：2026-09-13。基线：`df069415`。实现 C03 的阶段版本、输入水位和手动入口。

### 已实现

- 同一记录的纪要版本支持 realtime / quick / final，使用 Qwen3.8-Flash、严格输出校验和精确原文引用。实时/速记输出预算 4096 tokens，会后纪要 8192 tokens。
- 实时总结首次至少有 256 UTF-8 字节稳定文字，后续需间隔至少 60 秒且新增/变更稳定文字至少 256 字节。门槛在服务端锁内重查；读取进度和打开页面不会自动收费调用。
- 实时/速记生成使用不可变原文快照。生成过程中允许追加新发言，但原有输入被编辑、删除、转移或权限撤销时不发布。新的生成仍通过 generation/attempt 排除旧工作者，重复请求复用既有任务。
- 结束速记允许识别流尚未关闭；启用本批开关后，会后纪要等待所有已登记送达流不再 open。异常终止仍可生成带“不完整/未验证”状态的纪要，不把关闭流解释为全场覆盖完整。
- 阶段切换保存独立结果，不覆盖既有版本；已发起会后纪要后不再降回结束速记。公开接口不允许通过另一个浏览器替换正在生成的任务。
- 页面按实际可用阶段展示按钮，并标明草稿、原文观察水位和时间。旧前后端协议的默认 final 行为兼容。CLI 新增 --stage。

### 验证

后端原有选定回归 54 项通过，新阶段 10 项通过。新增测试首次失败原因是 active 场次夹具仍带 room_finished 结束原因，被数据库一致性约束正确拒绝；修正夹具后全部通过。覆盖追加/校正/删除/移源、关停、门槛、尾段、重试、旧 final 失效和阶段请求。

前端 15 项测试通过，TypeScript 和 ESLint 通过；相关 Python Ruff、Prettier、git diff --check 通过。0154 在隔离数据库迁移成功，迁移漂移检查无变化。未部署或执行真实模型/音频测试。

### 部署与后续

用户部署后端、迁移 0154 和页面后，可在原版本化纪要开关基础上设置 MEETING_STAGED_SUMMARY_ENABLED=true。默认关闭。关闭开关后不再发起新草稿，保留已有版本可读。新 Agent 仍按第七/八批配置。

当前是显式生成入口，尚未自动定时更新；仍使用全量已确认原文，超过 250,000 UTF-8 字节明确拒绝，不静默截掉开头。下一批实现可开关的自动总结调度和恢复，再继续长会分段预算与会中工作区。部署实测和阶段出口保持待验收。

---

<a id="phase2-batch4"></a>

来源：`meeting-ai-phase2-batch4-2026-09-13.md`。

## 会议 AI 阶段 2：第四批（累计第十批）

日期：2026-09-13。基线：`e7fbc607`。实现 C03 自动总结开关、服务端调度与恢复。

### 交付

- 当前会议管理员在记录页面明确开启自动总结后，服务端按稳定文字门槛生成实时总结；会议结束后依次生成结束速记与会后纪要。该开关只控制总结，不启动麦克风/Agent/云录制。页面关闭后调度继续。
- 开关以记录为单位，有独立控制 revision、调用人和幂等命令历史。旧开关请求重放只返回历史结果与当前状态，不重新执行，避免“关闭后旧开启请求又生效”。
- Celery Beat 每 15 秒检查，单次最多 100 条，按最久未检查排序。多个调度器由记录锁串行化；调度只创建持久请求，不内联调用模型。已有会后纪要与当前来源一致时不重复生成；迟到来源变化后可更新。
- 自动任务绑定开关 ID、revision 和发起人。调度、队列投递、模型调用前后重新检查权限；关闭/重新开启会使旧任务失效。关闭自动总结不取消独立发起的手动任务。
- 队列已发送但 30 秒仍未被工作者领取时，重发同一任务/attempt；不创建新任务。发送失败进入持久待发送状态，每次扫描最多重投 100 条。自己的 running 工作者超过 10 分钟转为可恢复失败，模型失败/无效输出不自动付费重试。
- 用户显式重试是新的手动 attempt，会移除过期的自动开关绑定；这样关闭自动总结后仍能手动重试失败任务。技术走查发现并补测了此边界。
- 新页面明确提示 AI 用量，重连后保留不确定请求的原 key 与 payload；只读分享者没有开关，灰度开关关闭时仍允许当前管理员停止已开启的自动总结。

### 验证

后端首次 55 项回归通过；新增手动重试边界后，自动总结与请求范围 30 项再次全部通过（本批新增 14 项）。覆盖并发调度、权限撤销、关闭竞态、幂等重放、自动三阶段、失败停留、工作者恢复及队列丢失恢复。

前端 20 项测试通过；TypeScript、ESLint、Prettier 通过。完整生产构建通过，仍有既有 marianne/devise SVG 运行时解析和大 chunk 提示。Python 相关 Ruff 通过，0155 在隔离数据库迁移成功，迁移漂移检查无变化。

### 用户部署检查

在已有记录、版本纪要、公开请求、分阶段开关及 CELERY_ENABLED 基础上，设置 MEETING_SUMMARY_AUTOMATION_ENABLED=true；执行迁移 0155，更新 API、worker、Beat 和前端。任务在既有 summary_versions 模块注册。也可运行 `python manage.py tick_record_summaries` 执行一轮同规则的有界调度。

全局开关不替任何记录开启自动总结。需要先有真实原文输入，再由管理员在页面开启。现有默认全部关闭；本批未部署、未开启真实记录、未调用收费模型、未发送 IM 消息。

下一批处理长会输入分段与阶段预算，再继续会中原文/总结工作区及独立录音、翻译线路。实际音频质量、端到端时延和队列负载由部署测试反馈验证，尚不宣称阶段 2 完成。

---

<a id="phase2-batch5"></a>

来源：`meeting-ai-phase2-batch5-2026-09-13.md`。

## 会议 AI 阶段 2：第五批（累计第十一批）

日期：2026-09-13。基线：`ee1abec3`。实现 C03 长会输入预算与可复用分段提炼。

### 处理方式

- 默认保持原单次生成预算；开启 MEETING_SUMMARY_CHUNKING_ENABLED 后，完整来源上限为 1,500,000 UTF-8 字节。单次输入不超过 250,000 字节的短会仍只调用一次。
- 更长来源按完整原文段落顺序分组，每组最多 64,000 UTF-8 字节、最多 32 组。不会截掉开头、跳过尾段或拆出虚构引用。单条过长、总量或分组数超过预算时，在创建任务前明确拒绝，原文保留；页面和自动控制状态显示来源预算问题。
- 每组提炼限制 1536 输出 tokens / 7000 序列化 UTF-8 字节；所有组提炼结果共同进入最终汇总，汇总输入最多 250,000 字节，输出保持实时/速记 4096、会后 8192 tokens。以上是本应用的保守预算，不是对供应商模型最大能力的声明。
- 组内提炼不包含阶段指令，按记录、精确源内容、模型、endpoint 和提炼 prompt 版本缓存。相同内容可以在实时/速记/会后和失败重试间复用，重新生成仍重新执行汇总。不同记录不共享缓存。
- 原文追加导致快照 revision 变化时，复用组的引用重新绑定到新快照；原文/说话人/语言/时间变化则该组缓存失效。所有组输出和汇总引用仍经严格校验，汇总不能引用未出现在提炼输入中的源 ID。
- 每段调用前、保存缓存前和最终发布前检查当前任务 attempt/generation、来源及权限/自动开关。段间写入工作者心跳，避免长会在持续工作时被误判为超时；前端可见已提炼进度。
- 版本纪要客户端关闭 SDK 隐式重试，失败由既有显式 attempt 恢复；生成响应必须以 stop 正常结束，不能把 length/content_filter 结束但恰好合法的 JSON 当作完成。其他 LLM 调用保留原默认行为。

### 检查与限制

后端 78 项通过，包括新增 10 项长会/缓存/响应检查用例：全源输入、全组汇总、失败后复用、校正重绑定、模型/记录隔离、原文变化取消、预算拒绝、坏缓存引用和不完整输出。前端 21 项通过；TypeScript、ESLint、Prettier、相关 Python Ruff、编译与 git diff --check 通过。迁移 0156 在隔离数据库通过，迁移漂移检查无变化。

自动测试验证流程和引用约束，不替代真实长会事实覆盖质量评测。分段提炼仍可能遗漏事实，需用户部署后用包含早期决策、后续推翻、未决问题的业务样本核验。旧人工/旧纪要接口保留；没有重写历史内容、创建任务或发消息。

用户部署 API、worker 和迁移 0156 后可打开本批开关，前端随同更新。此批未部署、未真实收费调用。下一批继续会中工作区、记录入口与新旧流水线的明确路由，再推进独立录音和翻译；完整阶段验收仍未完成。

---

<a id="phase2-batch6"></a>

来源：`meeting-ai-phase2-batch6-2026-09-13.md`。

## 会议 AI 阶段 2：第六批（累计第十二批）

日期：2026-09-13。基线：`8cc0f4ff`。实现 C04 会中阅读入口，并处理新旧纪要自动生成的路由。

### 实现

- 会议工具增加“会议笔记”，由现有 meeting_records.enabled 开关控制。已登录用户可切换本场文字记录与智能纪要，复用版本化纪要、来源引用和自动总结控制。
- 从当前 LiveKit Room.getSid() 取得场次身份，以 room_id + livekit_room_sid 精确解析记录。连接改变时清空旧 SID，迟到的异步结果不覆盖新连接；没有精确记录时等待，不回退到房间最近一场。
- 正式原文最近一页每 5 秒更新，按时间正序呈现，支持查看更早页和回到最新。每页最多 30 条；原有最早优先接口保持默认兼容。文字/纪要权限分别控制，撤销权限后不保留可见原文。
- 旧会议结束任务发现该记录已有版本化生成意图或自动总结控制记录时，跳过旧自动纪要。即使关闭新版开关/自动总结，也不会偷偷回退到旧自动生成。未选择新版的旧场次保留原流程；显式 force 的旧人工重生成仍保留原接口。
- 旧自动任务在会话锁后取得记录锁，检查并持有至其旧调用结束，串行化与新版选择的竞态。本批没有将旧摘要复制成新版、覆盖人工编辑或自动发送任何通知。

### 检查

后端 50 项通过（新增 6 项），覆盖精确 SID、同房间复用、错误组合、最新页游标、权限撤销、新版开启/关闭后排除旧自动调用和旧场次兼容。前端 19 项通过（新增 4 项），验证精确解析、顺序与翻页、只读分享、权限撤销和无回退。

TypeScript、ESLint、Prettier、相关 Python Ruff、git diff --check 通过。本批不新增迁移，未部署或真实录音测试。

### 下一批与部署提示

用户部署 API、worker 与前端，保留已有开关配置；只有当前身份具备资料权限才能阅读，LiveKit 参会令牌不自动授予资料权限。

这批是会中阅读工作区，尚未新增开始/停止采集按钮。现有 SubtitleService.stop_subtitle 尚未实现，且字幕与正式原文共用 Agent，需要继续补真实采集控制及收尾语义；不能只改按钮就宣称记录已停止。下一批围绕该边界实现，再继续独立录音与翻译。

---

<a id="phase2-batch7"></a>

来源：`meeting-ai-phase2-batch7-2026-09-13.md`。

## 阶段 2 第七批（累计第十三批）：在线采集控制服务

本批完成 C04 的后端控制契约；Agent 执行器和 Web 按钮继续下一批。新开关 `MEETING_ONLINE_CAPTURE_ENABLED` 默认关闭，不应单独开启本批后端并期望已经能够实际开始／停止采集。

### 已实现

- `GET/POST /api/v1.0/online-captures/control/` 要求 room UUID + 当前 LiveKit SID。开始前可读取控制状态，只有当前组织有效成员中的房间 owner/admin 能启动或结束；公开入会、历史参与和只读分享不授予控制权。
- 开始请求创建同一场次的会议笔记及一次采集运行，预留送达账本。用户幂等键保存原操作结果，返回最新状态；`expected_run_id` 防止旧页面停止新一轮采集。场次锁、记录锁和条件唯一约束保证一个活跃正式写入者。
- Agent 用独立进程 UUID 认领预留账本。后续原文、完成清单和心跳都必须匹配；旧的无账本写入及另开账本不能绕过已经进入新版采集的场次。旧链路已有开放账本时拒绝中途接管。
- 状态为 starting → recording → stopping → stopped / incomplete。停止请求不代表完成；已确认尾段可继续送达，只有通过连续序号及 ASR 观察校验的终态清单才确认结束。不会结束会议或操作云录制。
- 启动行兼作持久投递意图，LiveKit 调用在提交后执行，8 秒超时、15 秒重投间隔；多个投递只能有一个进程认领。既有 15 秒 Celery Beat 调度同时巡视采集。启动 60 秒、心跳 45 秒、停止 120 秒的超时均封存为 incomplete，阻止迟到写入，不能自动重新收费开一轮。
- 回退开关不截断现有运行的原文、心跳和完成上报。权限撤销后心跳要求收尾；自动纪要的旧流程跳过已经采用新版采集的记录。

### 验证

隔离 PostgreSQL 已应用新增迁移 `0157_online_capture_control`，没有生产迁移或部署。采集／送达／内部 API 24 项测试通过；会议笔记、统一记录、版本纪要、自动总结及阶段总结 72 项回归通过；迁移差异检查、变更范围 Ruff 和 diff 检查通过。

测试覆盖并发启动、幂等重放、未生成文字的空笔记、准确场次与控制权限、独占写入、旧写入阻断、停止时尾段、缺失序号拒绝成功、失联封存、迟到旧进程、开关回退、角色撤销、投递超时。LiveKit 投递使用模拟适配器，没有调用付费模型。

### 下一批及实测边界

Agent 必须先消费 dispatch metadata、认领账本并开始心跳，才可以订阅音轨进行转写；心跳停止命令需要驱动真实 drain，网络失联也必须主动关闭 ASR。本批尚未包含这部分执行器，不算完成 C04。

下一批接 Agent 执行及会中按钮，再衔接“记录停止但会议继续”的速记／最终纪要。部署时需同时升级后端与 Agent，再开启对应能力。既有旧 Agent 正在运行的场次保持旧路径直到自然结束，不在中途接管。端到端真实音频、设备、云录制并行以及退出尾段验证由用户部署后执行。

---

<a id="phase2-batch8"></a>

来源：`meeting-ai-phase2-batch8-2026-09-13.md`。

## 阶段 2 第八批（累计第十四批）：Agent 执行与会中采集按钮

承接第十三批在线采集控制，完成 Web → 精确场次控制 → LiveKit dispatch → Agent 认领 → 心跳 → 尾段收齐 → 结束确认的代码接线。云录制不受开始／结束文字记录操作影响。

### 实现

- 会中“会议笔记”增加文字记录状态与开始／结束按钮；即使尚未生成第一句原文也能开始。仅有当前控制权限的用户显示按钮，已有记录的授权读者可见运行状态；撤权后清除缓存显示。
- starting、recording、stopping、stopped、incomplete 分别显示。停止后等待 Agent 确认，不能提前显示已结束；失败重试复用相同操作键和 run ID，后续状态查询也失败时仍可重试不确定操作。
- Agent 消费后端 dispatch 的 delivery UUID 和 LiveKit SID，在准确场次认领独占进程 UUID。首次心跳确认 recording 后才启动 ASR；停止已先到达则直接收尾空运行，不启动模型。注册失败不再回退旧无账本写入。
- 心跳每 2 秒进行，单次 HTTP 超时 3 秒，连续 15 秒无法确认控制状态则停止采集并标记 incomplete。停止时持续心跳直到真实 drain 与完成上报结束；显式停止与框架退出共用一次收尾锁。
- 重复 managed dispatch 使用独立 SFU 身份，落败进程由数据库认领拒绝，不会先挤掉正在运行的连接。旧字幕启动入口也不再并行加入已有 managed Agent；原文采集排除 Agent 和 Egress 音轨，为译音防回流提供基础。
- 内部转写与心跳 HTTP 禁止跟随重定向，防止共享凭据被转发到另一地址。

### 验证与边界

Agent 全部 37 项单元测试通过，覆盖进程身份贯穿原文／尾段、认领失败、错场次、重复调度、网络失联、收尾续租、并发退出、机器人音轨和 HTTP 回包验证；均无真实模型调用。Web 会中记录与控制 9 项测试通过，TypeScript、ESLint、变更范围 Ruff 和生产构建通过。构建仍有既存 Marianne/Devise SVG 运行期解析及大包提示。

后台控制与 Agent 必须同时升级后，再开启 `MEETING_ONLINE_CAPTURE_ENABLED`、`MEETING_RECORDS_ENABLED`、`MEETING_TRANSCRIPT_DELIVERY_ENABLED`，并运行 Celery Beat。正常旧字幕链路可继续保持原配置；managed dispatch 强制启用送达认领，不依赖 Agent 的旧可选账本开关。生产 ASR 使用阶段 0/2 已登记的 Qwen 配置，实际密钥仍由部署注入。

尚未进行真实 LiveKit/麦克风/模型端到端验收。下一批继续补“结束记录但会议继续”的速记与完整纪要时机、面向参会者的采集状态提示，以及控制边界的技术走查。独立录音、Android 对齐和翻译流水线仍在后续计划内，不将本批描述为全部阶段开发完成。

---

<a id="phase2-batch9"></a>

来源：`meeting-ai-phase2-batch9-2026-09-13.md`。

## 阶段 2 第九批（累计第十五批）：记录结束边界与参会提示

本批补齐“结束文字记录但会议继续”的总结时机，以及所有参会者可见的采集提示。仍未进行生产部署或真实音频验收。

### 完成内容

- stop 请求进入 stopping 后，允许针对已收到原文生成速记；送达账本封存前不允许 managed capture 生成最终纪要，即使关闭阶段总结开关也不能绕过。stopped/incomplete 后可基于已保存材料生成完整结构的纪要，并继续保留覆盖范围未验证或送达不完整提示。
- 每个新总结任务固定当前 capture run ID。再次开始记录会取消上一轮排队或运行中的旧总结，防止旧草稿跨新一轮发布；已经发布的历史版本保留，同场笔记不重复创建。
- 自动总结按当前记录运行识别速记版本。修复“第一轮已有最终纪要，第二轮停止时没有新速记”的边界；每轮仍须具备已有明确自动总结授权，开始采集本身不自动开启总结。
- 新增只读 `GET /api/v1.0/meeting-capture-status/`，要求有效 LiveKit room-join token 和准确 room/SID，只返回当前活动场次的状态字段。匿名参会者可获知是否在记录，但不能由此读取原文、纪要、run ID 或控制录制；历史场次不对该接口开放。
- 会中增加独立文字记录状态提示，断线后清除当前 SID；已观察到记录开启而状态查询失败时显示“无法确认”，不谎称已停止。与现有云录制提示分开显示。
- 新增开始操作限流，每用户 6 次/分钟，不限制结束操作；成功认领后清除此前投递失败提示。

### 验证

后端采集边界、采集控制、阶段总结、自动总结、版本总结与送达共 65 项测试通过；前端采集提示、采集控制、会中笔记、自动总结共 17 项通过。TypeScript、ESLint、Ruff、迁移差异检查及前端生产构建通过；构建仅保留原有 SVG 和大包提示。没有新增数据库迁移或真实付费模型调用。

### 后续

继续 T01 Qwen 翻译协议迁入，再接翻译控制与音轨路由；独立录音和 Android 按总计划继续。阶段 2 的真实采集效果、声道与说话人、云录制并行、停止尾段、浏览器和原生设备生命周期仍需部署后测试；仅文字保留承诺须在音频临时数据清理验证后开放，不因当前 Agent 未主动上传音频而提前宣称完成。
