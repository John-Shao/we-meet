# 妙记整改：目标环境验收清单

代码批次与验证证据见 [执行状态](miaojie-execution-status-2026-09-20.md)。本文件列出尚未完成的目标环境验证，不代表已经部署、传输成功或模型质量达标。

## 开始所需信息

- 已部署本轮后端/Web/Android 提交的测试环境地址及版本标识。
- 可用的测试登录方式；至少所有者、仅纪要读者、全文读者三个测试身份。不要把密码、令牌或云存储密钥放入本文件。
- 可用于测试的媒体或记录：约 47 秒竖屏视频、超过 30 段的长录音、大于 100 MiB 的文件。截图不是可上传或解码的媒体样本。
- ASR、LLM、对象存储和需要测试的 Worker 在该环境中可用。

2026-09-20 用户提供生产 `https://meet.we-meet.online`、部署版本 `f2e7f3cd4`（Helm revision 371）、演示身份与两个样本。已使用该身份进行下述真实 API 验收；浏览器自动化连接连续失败，未取得页面状态。没有执行生产部署。只有所有者身份，权限分离场景尚未实测。

## 本轮真实 API 证据（北京时间 09:40–09:52）

| 样本 | 大小 / SHA-256 | 新建测试记录 |
|---|---|---|
| `test-audio01.m4a` | 2,322,932 字节；`3a1168fb2ac542342b41f088af473e108dfded0e9d90ac127a1e91c37dd0326c` | `10d6f0af-f086-46d0-8100-ebd60935a482`，标题 `验收20260920-音频` |
| `test-video01.mp4` | 19,855,680 字节；`77601fd6492ebd398a0e2dfa5b5914d14c53ff5266dd5e8667469d7f6ad73bdd` | `5233041b-a706-404d-a232-3b40903179c1`，标题 `验收20260920-视频` |

- **阻断**：能力接口宣告 direct upload 可用，但签名接口有效请求两次 HTTP 500，未发生 PUT。第十三批本地真实 PostgreSQL 复现事务异常并修复；生产错误未取得堆栈，须部署后复测。
- **通过（API）**：普通 multipart 表单上传两样本均 202，ASR 均 succeeded；音频 18 段，最后片段结束 91,970 ms；视频 8 段，最后片段结束 47,340 ms。这是识别时间范围，不是媒体完整时长或识别准确率证明。
- **通过（API）**：两记录改名 200；音频 80,000 ms 定位返回覆盖该位置的 79,370–81,130 ms 片段。未测试超过 30 段跨页。
- **通过（API）**：所有者原文件下载 200，attachment 文件名正确、两文件完整字节数及 SHA-256 与本地样本相同；两个后段 Range 均 206、1024 字节，Content-Range 正确。匿名获取媒体签名 401。未测试浏览器下载流程或签名 TTL 续期。
- **通过（API）**：音频首段校对成功、旧校对版本写入返回 409；有效文本搜索及 TXT/SRT/VTT 导出包含测试标记；原纪要快照文字不变，新生成纪要的输入快照含校对文字。验收后追加校对恢复原文，保留历史版本作为审计，因此校对测试中的纪要版本会显示来源已变化。
- **通过但不代表质量验收**：音频生成两份纪要；第一份纪要 11 个引用与固定输入快照的片段 ID、版本、起止时间全部匹配。ASR finished、delivery complete；coverage unverified，作业 partial。没有以模型完成推断语音完全覆盖。
- **恢复成功 / 待追因**：视频纪要第一次 `provider_unavailable`，作业 `773b9177-1045-4852-9146-d18ff86d20eb`；北京时间 09:49:47 产品内重试获 202，09:50:17 生成版本、状态 partial，错误码清空。首次失败原因待 summary worker 脱敏日志，不连续重试掩盖失败率。
- 发言统计接口返回音频未知说话人、识别片段总时长 83,470 ms、占比 100%。该样本默认未启用分人，不能据此认定说话人识别准确；重叠/不完整时间样本未实测。代码核查显示服务端支持 `diarization` 但默认 false，当前 Web 上传只提交 context/hotwords，没有分人选项。这是下一轮应补齐的实际产品缺口，不能将统计组件已实现等同于上传件已具备自动分人能力。

仅新建和修改以上测试记录，没有删除原有记录或授予其他人员访问权限。凭据与签名 URL 未写入文件。两样本均小于 100 MiB，未覆盖大文件分片分支。

## 操作与通过条件

| 场景 | 操作 | 必须观察的结果 | 证据 |
|---|---|---|---|
| 短竖屏视频 | 导入、播放、查看全文/纪要/章节、改名 | 画面比例正确；无章节有明确空态；改名在两端一致 | 普通上传/全文/纪要/改名 API 通过，生成 3 个章节；UI/解码/无章节空态未实测 |
| 长原文 | 播放越过 30 段；搜索另一页；点击原文/章节回听 | 高亮、跨页定位与媒体时钟一致；手动阅读期间不抢滚动 | 待实测 |
| 并发校对 | 两端打开同一段，分别修改；另一端刷新/保存 | 草稿保留；旧版本保存冲突；搜索/新纪要/导出使用有效文本，旧引用不变 | 版本冲突、搜索/导出/新旧快照 API 通过；双端草稿 UI 待测 |
| 分片恢复 | >100 MiB 上传，在合并请求后制造响应丢失 | 重新进入后识别已合并对象，免重传并且只建一条记录 | 待实测 |
| 媒体续期 | 播放超过签名 TTL，再定位远端片段 | 获得新签名；位置/倍速/暂停状态保留；实际 Range 请求成功 | 待实测 |
| 原文件下载 | 上传所有者在 Web/Android 信息页下载 | 服务端 attachment 文件名正确；浏览器完成传输；文件大小/哈希一致 | HTTP 下载两样本附件名/大小/哈希通过；双端页面下载待测 |
| 纪要/全文分享 | 分别授予、撤销两种范围，再重试旧请求 | 未选权限不变；无媒体权限提升；仅纪要不能读全文；回放旧请求不恢复已撤权授权 | 待实测 |
| 撤权 | 打开原文与媒体后撤销对应权限，再刷新或等待复核 | 不再请求/展示被拒绝内容，不再获取新媒体签名 | 待实测 |
| 发言统计 | 使用有重叠、未知发言和不完整结束时间的已知样本 | 与“识别片段时长”口径一致；部分/无数据有说明；不是录音总时长占比 | 待实测 |

记录实际版本、身份角色、样本标识、网络条件、结果和失败复现步骤。签名 URL 和令牌不得作为报告附件；需脱敏后保留请求状态、Range/附件响应头和时间戳。

精细发言时间轴、批量校对、长期热词、关键词、全局筛选排序仍在产品路线图中。媒体共享、原生分片下载/拼接和线上会议完整录像回放尚需专门设计与验收，不包含在“上传所有者原文件下载”中。
