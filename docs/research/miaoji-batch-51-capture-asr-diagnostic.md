# 第五十一批：非空历史删除验收前置阻塞与只读诊断

## 本次结果

2026-09-21，沿用 Helm 383。为补第 50 批未覆盖的非空 AI/人工历史删除，使用用户提供的 `test-audio01.m4a` 前 30 秒，转换为 16kHz 单声道 PCM16，创建独立可销毁原生录音。未修改旧回归样本，未使用内部写入凭据伪造原文。

- 标题：`miaoji-b51-disposable-history`。
- record：`c6e57fed-e82f-4992-a4e0-2ee93c3b6d35`。
- capture：`009d0729-b622-47e7-9b10-09b08e775b05`。
- ASR job：`e889ad59-a1d3-4e5f-88f6-d56a94bc1685`。
- 三个 10 秒分片上传、封存、停止成功，媒体状态 `saved`。
- 通过所有者公开 API 请求 sealed ASR，任务实际结束为 `incomplete`，错误码 `provider_or_delivery_incomplete`；`acknowledged_inputs=3`、`input_count=3`、`final_count=0`，原文读取为 0 段。
- `summary-job/` 返回 `generation_ready=false`、`ready_stages=[]`，没有创建 AI、人工历史或导出。因此没有执行本样本的回收/永久删除，也不能声称非空历史级联通过。

样本保留在正常库供诊断，尚未自动重试。此结果说明本次原生转写链路存在失败，不能据公开聚合错误码断言供应商故障、存储故障或永久删除回归。第 50 批原生删除的既有通过结论保留；上传样本仍须北京时间 **2026-09-21 10:49:50** 后复核。

## 下一步：运行只读诊断

新增 `deploy/aliyun/check_capture_transcription.py`，在生产主机执行：

```bash
git pull
python3 deploy/aliyun/check_capture_transcription.py \
  --job e889ad59-a1d3-4e5f-88f6-d56a94bc1685
```

脚本读取 backend 与 capture-asr deployment 的镜像及就绪副本数，在 backend 内通过 PostgreSQL 只读事务查询这一任务。输出任务是否开始、结束回执是否存在、供应商完成标记、预期/实际任务数、音频采样数、输入确认数及最终文本计数；实时任务使用实际输入台账。它不调用会改变过期状态的业务 state()，不重试任务、不请求供应商、不读取/输出原文、音频、完整 report、凭据或签名 URL。

回执只能缩小失败阶段，不能单独证明具体供应商错误。依据回传结果再决定是否需要进一步有界诊断或修复。不要删除此样本或批量重跑付费任务以覆盖失败证据。

## 验证与发布

- 5 项独立单元测试通过：完整输入但供应商未结束、缺失结束回执、音频/文本计数不一致、实时输入台账及命令失败输出脱敏。
- Ruff 格式与静态检查通过；命令帮助和 Git whitespace 检查通过。
- 本地没有生产 kubectl 上下文，脚本尚未在生产执行，不能将本地测试记为生产诊断完成。
- 本批只有运维诊断脚本、测试和文档，无应用/配置/迁移变更，无需构建镜像或 Helm 发布。

修复前置转写问题后，继续同一样本的非空纪要历史与外部文档边界验收；上传到期最终清理、旧上传键重放保持单独待办。大文件恢复和长原文跨页继续按用户要求延后。
