# 第三十三批：日期筛选与只读导出提示生产验收

2026-09-20，用户部署 backend/frontend `85c0291d6`（20:21:47，Helm revision 378）；summary/agents 仍为 `f2e7f3cd4`。使用 Pixel 8 / Android 16、Android `d5796c85` debug APK 和生产 API 验证第 31/32 批。

## 已验证

- API 返回日期筛选能力声明；09 使用创建日期 × owned × upload × 标题定位已有验收记录。以记录真实 `created_at` 为开始边界仍包含，以它为结束边界则排除；未来日期为空，无时区日期/倒置范围返回 400。
- 08 未授权时列表为空；临时授予该验收记录的纪要只读权限后，shared × upload × 日期 × 标题查询准确返回该记录，导出列表 `available=false`。
- App 登录 08，在“分享给我”组合导入来源和 2026-09-20 单日范围，找到该记录；添加标题搜索，改为 2026-09-21 后显示无匹配记录。清除日期后标题/来源保留，记录重新出现。
- App 倒置范围 2026-09-21 → 2026-09-20 被拒绝，未离开筛选表单。发现错误内容使面板回到半展开状态，提示落到屏幕下方；进入第 34 批修正面板展示，不将该 UX 问题描述为通过。
- 只读 App 信息页显示“尚未导出文档”，不再提示创建，且不显示所有者的 12 份私有回执。
- 临时分享已撤销，再读 owner 的授权列表为空；08 详情返回 404，带日期的 shared 查询也为空。没有创建记录、上传文件、编辑原文/热词/纪要或新增 Docs。

## 边界

只使用既有验收记录 `61307ff3-8aa2-47fe-ab9c-e4e4111947fd`（miaoji-b30-20260920-audio）。App 设备使用 UTC，API 另验证 +08:00 边界；没有在生产切换设备时区或构造夏令时数据。组合筛选真实多页、进行中样本、Web 页面与物理设备仍未实测，相关本地回归不能替代这些生产场景；大文件恢复、长原文跨页继续延后。Android 正式分发仍待完成。

本批为验收记录，无部署操作和运行代码变更。第 31/32 批的部署前置已完成，后续不再以旧版本为由标记阻塞。
