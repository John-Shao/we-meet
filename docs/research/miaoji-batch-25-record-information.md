# 第二十五批：记录信息与个人纪要文档导航

本批补齐 G6 中可直接复用现有契约的部分：所有者、创建时间、已导出文档和 AI 来源版本导航。批量校对、个人热词、发言时间分布仍独立排期，不把本批记作 G5–G8 全部完成。

## 变更

- Web/Android 记录信息页展示所有者和创建时间。创建时间使用 `created_at`，与已有来源开始时间 `origin_at` 分开；旧后端未给出创建时间时显示未知，不拿来源时间冒充。Android DTO 增加可选字段，兼容既有记录。
- 信息页增加“我的纪要文档”，直接复用已有 document-exports 读取接口，显示当前用户最近 10 次导出的类型、时间、语言和处理状态。准确说明这是个人导出范围，不声称列出全体参与者的文档或所有历史导出。
- 仅当 `status=ready`、`can_open=true` 且有文档 ID 时提供 Docs 入口。即使当前不再允许创建新导出，已有可打开的文档仍能导航。读取信息页不会自动创建、重试导出，也不发送通知。
- AI 导出可回到准确的 `source_id` 纪要版本：Web 使用现有版本路由，Android 切换到该版本并清除上一次引用/管理面板。文档仍是独立副本，没有建立双向内容同步或共享权限继承。
- 无 `read_summary` 时不请求导出接口；账号进入缓存键，刷新错误不显示缓存链接。Android 沿用前后台复核并清除旧内容，信息页整体可滚动，避免增加条目后底部操作不可达。

## 验证

- Web：信息页/导出导航/既有导出控制共 38 项通过。新增覆盖只读全文身份不读文档、无创建行为、精确 AI 版本路由、不可打开状态、账号切换、撤权刷新后移除缓存链接。
- TypeScript、修改文件 ESLint、120 个 JSON 解析、五种 Web 语言结构及占位符检查通过。Web 五种语言和 Android 中英文均补齐文案。
- Android：Pixel 8 / Android 16 上 `RecordExportsTest` 共 9 项通过，含原有导出行为及新增信息页读取、文档/版本回调、无纪要权限不读取、撤权清理。为取得布局截图单独复跑信息页用例通过（不重复计入测试总数），人工检查信息行、文档状态与两个导航按钮无遮挡。提交设计 token 检查通过。
- 测试使用受控夹具；本批没有写入生产记录、导出生产 Docs、修改共享权限或调用 ASR。不能将模拟器导航回调断言说成实际生产 Docs 全链路已经验收。

## 发布与剩余边界

Android `98a20dba` 已推送，debug APK 已安装到模拟器；正式分发待完成。用户于 2026-09-20 14:56 发布 frontend `d6c7de6c7`，Helm revision 376；backend 仍为 `66b3e3bb3`，summary/agents 为 `f2e7f3cd4`。本批没有 backend、summary、agents 或数据库迁移变更。

部署后实际结果见下节；Web 实际页面、物理 Android、仅全文身份的生产 App 信息页以及导出版本早于最新纪要的对照场景仍待验收。

G6 剩余：可靠的媒体总时长及多来源处理信息、超过 10 次导出的历史导航、人工纪要历史版本直接定位。不能用最后一条转写结束时间代替媒体总时长，也不能把识别发言人当参会者。G5 批量校对/可复用热词是后续复核效率批重点。用户延后的大文件恢复与长原文跨页保持延期。

## 部署后生产验收（2026-09-20，约 15:00–15:09）

使用 Pixel 8 / Android 16、Android `98a20dba` debug App 和用户授权的演示所有者/第二账号。仅操作此前验收创建的视频记录 `edefb542-e50a-4851-964e-2682961939be`（`miaoji-app-20260920-video`）。

- **信息页通过**：生产接口返回所有者与 `created_at=2026-09-20T02:59:26.430989Z`；App 显示对应所有者、创建时间、来源开始时间及保留方式。模拟器当前时间显示为 UTC，与该时间戳一致，不把设备显示时间误记为北京时间。
- **真实导出通过**：初始个人导出列表为空、available=true。从 App 的“管理纪要 → 选择版本 → 预览 → 确认创建”导出一份英文标题语言的 AI 纪要副本，没有重新生成纪要或调用 ASR。导出 `410492bb-b66d-4cfa-bda3-893484d2b95f`，15:03:55 创建；status=ready、attempt=1、can_open=true、错误码为空，个人列表只有此一条。
- **信息页到 Docs 通过**：滚动信息区后可见状态和“打开文档”；进入原生 Docs，加载标题、Overview、Topics 和来源时间等实际正文。文档 ID `3bd5647e-c120-40a2-8707-6df64b4e8a09`；所有者通过独立 Docs 会话读取 HTTP 200，标题匹配。没有将只进入空白 Docs 页面当作成功。
- **来源版本导航通过**：信息页的 AI 来源入口进入“正在查看链接指定的版本”，显示 03:01（UTC）生成的最终纪要；导出记录绑定 `source_id=7626b7c9-2d74-477c-b4ea-d46df9725627`。当前测试记录只有该纪要版本，尚未验证“已有更新版本时仍打开旧版”的生产对照；精确 ID 传递已在本地回归覆盖。
- **跨系统独立权限通过（真实 HTTP）**：第二账号建立自身 Docs 会话，未分享时读取文档为 403、读取记录导出列表为 404。临时仅授予本测试记录的纪要权限后，记录 read_summary=true、read_transcript=false、play_media=false；个人导出接口返回 200、available=false、results=[]；直接读取所有者文档仍为 403。说明纪要分享没有继承成 Docs 权限，也没有泄漏所有者的导出列表。
- **清理完成**：临时纪要授权已撤回，显式分享列表为空，第二账号再次读记录为 404。仅保留这一份私有测试文档及对应导出回执作为验收记录；未修改其他生产记录、未发送消息，令牌/cookie/会话 URL 未保存到报告。

未发现新的运行代码问题；本次只有验收文档更新，无需再次部署。Android 正式分发、Web 页面及前述剩余场景仍单列待验收。
