# 第四十二批：逻辑回收启用配置

## 已满足的前置

第 41 批已记录 backend/frontend `bd16ac487`、backend worker/beat 的成功 rollout，并从生产确认生命周期列可查询、普通记录可读取、关闭开关时拒绝回收。旧音视频时长、新音频原始时长和人工历史来源完成所列生产验收。第 39/40 批后端及双端逻辑回收回归已经通过。

## 配置与验证

生产 values 将 `backend.envVars.MEETING_RECORD_TRASH_ENABLED` 从字符串 `False` 改为 `True`。这是全局能力开关，部署后符合条件的上传/录音所有者均可使用；不是测试账号白名单。双端既有入口通过服务端 capability 显示，无须重新发布 frontend。

使用假凭据覆盖缺失的 Secret 输入后，生产 values 的 Helm template 渲染通过。解析 Deployment 中的实际 env，断言 `meet-backend`、`meet-celery-backend`、`meet-celery-beat` 均获得字符串 `True`。未连接或修改生产 Kubernetes；没有新增迁移或永久清理任务。

## 发布与待验收

用户执行 `bash deploy/aliyun/release-meet.sh backend` 并确认三个 backend 进程 rollout 后，进入下一批生产验收：

1. 仅用第 41 批新建的 `miaoji-b41-20260920-audio`（`9a07360f-3a39-420a-abe8-7ca0b87a229a`）验证 App 确认/取消、回收后离开详情、库和原文/纪要/媒体续签接口拒绝读取、回收站元数据与恢复。
2. 临时分享只授予测试读者；回收撤销授权，恢复不自动复活授权。恢复后的所有者正文和媒体仍可读取，修订版本不被生命周期版本替代，过期反向操作不能重放。
3. 验证处理中拒绝回收与自动纪要关闭/不重启；不为测试发送真实通知，不创建永久删除或过期清理任务。结束时恢复测试样本并撤销临时分享。

永久删除、保留期自动清理仍未实现。已有签名 URL 在 TTL 内可能继续有效，不把禁止续签描述为立即撤销已签名 URL。外部 Docs 副本仍按其独立权限访问。

若需关闭新操作，将该开关改回 `False` 并发布 backend；这不会自动恢复已回收记录，统一读取过滤继续生效。后续恢复需在启用的授权流程下显式执行，不通过关开关隐式恢复。

本批仅准备并推送配置，生产仍为第 41 批确认的关闭状态，回收/恢复尚未生产验收。
