# 第六批：分片上传完成结果未知时的恢复

日期：2026-09-20。

- 对象存储已合并但响应丢失时，通过服务端分配的对象键读取 HEAD，核对大小、Content-Type、上传意图标记并检查真实文件头，再登记任务。同一 session 重试只返回同一个作业。
- 新会话在对象元数据记录意图摘要；历史会话兼容没有该标记的对象，仍必须通过服务端对象键、大小、类型和文件头校验。
- 恢复 GET 只返回 `completion_pending`，不创建收费作业；Web/Android 随后显式 POST 完成登记，无需重新打开文件或传输分片。Android 也能解析 begin 返回的已有作业。
- 任务登记失败保留文件及 OPEN 会话，重试可恢复；完成与取消使用相同的用户锁顺序，取消已合并但未登记的对象会清理媒体。已登记作业不能通过取消会话删除。
- 修复 Web 每批签名后上传进度从旧值重新累计的问题。

验证：后端分片上传 28 项通过（128 MiB 声明、响应丢失、数据库失败、恢复只读、重复完成、文件大小/文件头/意图/权限拒绝、取消清理）；Web 分片上传 15 项通过、TypeScript/ESLint 通过；Android 分片仓储 14 项通过，另一个慢响应 HTTP 测试通过。

没有数据库迁移。存储测试使用受控替身，不代表完成真实 128 MiB/6 GiB 传输或生产 OSS/S3 的 CORS、Range、超时验收。
