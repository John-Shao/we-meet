# 质量与故障诊断：收尾清单

更新：2026-09-21。用户授权按项自动推进、完成后提交并推送；遇到需要部署等阻塞可停下。本清单只跟踪质量与故障诊断，不占用共享批次号，不将其他窗口的跨记录检索评测合并计算。

## 当前结论

**专项尚未全部完成。当前下一项被生产部署阻塞。**

- [第一阶段](miaoji-asr-quality-diagnostics-2026-09-21.md)：六个冻结合成音频及首次供应商结果已保存；存储、转写、结果获取/解析、回传及清理故障已注入验证；正常转写和轮询失败已有 Helm 389 生产证据。
- [无语音分类与提示](miaoji-asr-no-speech-2026-09-21.md)：实现、双端回归、真实供应商静音复测、镜像及 Android debug 构建已完成。Web/服务端最终代码 `ae4db984b`，Android `6f372546`；代码已推送。149 项本地相关测试不等于生产页面验收。
- 生产侧尚未收到 `ae4db984b` 部署回执。当前可确认的旧任务使用 backend `4c5ca713f` / agents `c92b7408e`；不推断未观察到的后续部署状态。

## 自动推进顺序与完成标准

| 顺序 | 事项 | 已有证据 | 完成标准 / 依赖 |
|---|---|---|---|
| 1 | 无语音生产与 App 验收 | 实现、149 项本地测试、实际供应商错误码及三镜像 | 部署固定 tag 后，新建静音任务应 incomplete + no_speech_detected + 0 原文；正常语音成功；失败重试保留旧原文；App 可见解释且不自动重试。历史失败不改写。依赖生产部署及更新测试 App。 |
| 2 | 固定样本的纪要/待办事实基线 | 六样本音频、脚本及五份成功转写；尚无对应纪要/待办产物 | 冻结逐项标准，分别比较校对原文→纪要与 ASR 原文→纪要；记录事实、否定、责任人、相对截止时间、遗漏、无依据补充及引用对应。保留首次输出和失败，不把关键词命中当事实正确。 |
| 3 | 真实多人、术语、噪声和交叠基线 | 合成轮流双声线、中文术语、白噪声已测；当前没有人工标注的真实多人会议基线 | 使用来源允许且参与者知情的真实样本，固定音频哈希、人工校对文字和发言区间；覆盖安静轮流、真实噪声、交叠。缺失/未知身份单列，不能报告虚假的 DER。现有单段音视频和 TTS 不替代此项。需要适当真实样本及标注。 |
| 4 | 原文忠实度与术语回归 | 英文干净样本一段自动变中文；中文“妙记”→“妙计” | 冻结首次结果，针对同一音频做有限、可追溯的候选对照；分别报告原语言保持、专有词、金额/否定事实，不能用重跑最好结果覆盖基线。不预设必须更换供应商或增加热词功能。 |
| 5 | 持久诊断历史和查询边界 | 脱敏阶段日志及 24h/2000 行只读探针；无语音枚举进入结束回执 | 补齐结构化、受权限保护且有保留上限的阶段故障记录；覆盖 Pod 重建后可查询、未知字段过滤、重复事件与取消语义。不因日志窗口为空认定成功。其余故障目前不能宣称已持久化。 |
| 6 | 总体验收关闭 | 各项证据分散，未整体关闭 | 汇总每个用例的固定输入、代码/部署版本、输出、判定和遗留项。明确本地注入、真实供应商、生产 API、App、真人质量各自范围；只有对应证据齐全的事项才能关闭。 |

原生自动分人、组织/实时热词是独立产品能力，质量评测会暴露并记录其影响，不因为本专项授权就宣称已经实现。大文件恢复和长原文跨页继续遵照用户先前要求延后，不纳入本轮自动恢复验收。

## 当前阻塞的解除

三镜像已经推送，无新迁移或开关变更。在生产主机按顺序执行：

```bash
bash deploy/aliyun/release-meet.sh --tag ae4db984b backend frontend
bash deploy/aliyun/release-meet.sh --tag ae4db984b agents
```

收到成功回执后自动进入第 1 项，使用独立测试记录和已授权演示账号；通过后继续后续项目。需要服务器日志时使用已提交只读探针，不要求重复提交已有代码，不把文档提交 SHA 当成镜像 tag。Android 使用已构建的 `6f372546` debug APK；正式分发另记状态。
