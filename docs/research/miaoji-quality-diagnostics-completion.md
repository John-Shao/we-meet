# 质量与故障诊断：收尾清单

更新：2026-09-21。用户授权按项自动推进、完成后提交并推送；遇到需要部署等阻塞可停下。本清单只跟踪质量与故障诊断，不占用共享批次号，不将其他窗口的跨记录检索评测合并计算。

## 当前结论

**专项尚未全部完成。Helm 390 无语音生产 API 已通过；Helm 391 预算历史约束生产复核完成。持久阶段诊断已实现，待 backend/agents 部署及新任务验收。App 画面和真人标注样本仍有阻塞。**

- [第一阶段](miaoji-asr-quality-diagnostics-2026-09-21.md)：六个冻结合成音频及首次供应商结果已保存；存储、转写、结果获取/解析、回传及清理故障已注入验证；正常转写和轮询失败已有 Helm 389 生产证据。
- [无语音分类与提示](miaoji-asr-no-speech-2026-09-21.md)：实现、双端回归、真实供应商静音复测、镜像及 Android debug 构建已完成。Web/服务端最终代码 `ae4db984b`，Android `6f372546`；代码已推送。149 项本地相关测试不等于生产页面验收。
- 用户提供 Helm 390 / `696a7768f` 成功回执，运行时代码包含 `ae4db984b`。新静音返回 no_speech_detected，普通中文成功，复查均只有一次任务。App debug 已安装，但深链自动审批返回 blocked by policy，画面未验收。
- [纪要事实质量](miaoji-summary-fact-quality-2026-09-21.md)：保留四份生产首次纪要与六份候选输出，发现并定向约束否定金额被扩写为历史决策；38 项回归通过。条件事件化、否定遗漏、术语/语言保持仍有缺口，不将结构正确等同事实通过。

## 自动推进顺序与完成标准

| 顺序 | 事项 | 已有证据 | 完成标准 / 依赖 |
|---|---|---|---|
| 1 | 无语音生产与 App 验收 | 实现、149 项本地测试、Helm 390 新静音/普通语音 API；App 已安装但审批阻塞 | 部署固定 tag 后，新建静音任务应 incomplete + no_speech_detected + 0 原文；正常语音成功；失败重试保留旧原文；App 可见解释且不自动重试。历史失败不改写。依赖生产部署及更新测试 App。 |
| 2 | 固定样本的纪要/待办事实基线 | 六样本音频和首次转写；已有四份中英文生产纪要、六份候选输出及逐项事实评审（Codex），其他噪声/交叠结果尚未扩展到纪要 | 冻结逐项标准，分别比较校对原文→纪要与 ASR 原文→纪要；记录事实、否定、责任人、相对截止时间、遗漏、无依据补充及引用对应。保留首次输出和失败，不把关键词命中当事实正确。 |
| 3 | 真实多人、术语、噪声和交叠基线 | 合成轮流双声线、中文术语、白噪声已测；当前没有人工标注的真实多人会议基线 | 使用来源允许且参与者知情的真实样本，固定音频哈希、人工校对文字和发言区间；覆盖安静轮流、真实噪声、交叠。缺失/未知身份单列，不能报告虚假的 DER。现有单段音视频和 TTS 不替代此项。需要适当真实样本及标注。 |
| 4 | 原文忠实度与术语回归 | 英文干净样本一段自动变中文；中文“妙记”→“妙计” | 冻结首次结果，针对同一音频做有限、可追溯的候选对照；分别报告原语言保持、专有词、金额/否定事实，不能用重跑最好结果覆盖基线。不预设必须更换供应商或增加热词功能。 |
| 5 | 持久诊断历史和查询边界 | 新增每任务 20 条、30 天持久历史，owner/worker 权限、取消及过期清理已回归；待生产部署 | 补齐结构化、受权限保护且有保留上限的阶段故障记录；覆盖 Pod 重建后可查询、未知字段过滤、重复事件与取消语义。不因日志窗口为空认定成功。新代码尚未部署，不能宣称生产其余故障已经持久化。 |
| 6 | 总体验收关闭 | 各项证据分散，未整体关闭 | 汇总每个用例的固定输入、代码/部署版本、输出、判定和遗留项。明确本地注入、真实供应商、生产 API、App、真人质量各自范围；只有对应证据齐全的事项才能关闭。 |

原生自动分人、组织/实时热词是独立产品能力，质量评测会暴露并记录其影响，不因为本专项授权就宣称已经实现。大文件恢复和长原文跨页继续遵照用户先前要求延后，不纳入本轮自动恢复验收。

## 当前阻塞的解除

旧无语音与预算约束已分别随 Helm 390/391 部署。纪要复核发现未知责任人占位仍不稳定、条件性表述仍有风险，见[生产结果](miaoji-summary-fact-quality-2026-09-21.md)。下一部署为[持久阶段诊断](miaoji-asr-durable-diagnostics-2026-09-21.md)：先 backend 及迁移 0188，再 agents；之后验收新任务的数据库历史。

App 深链被自动审批拒绝（blocked by policy），不能把 API 验收当成画面通过。已向用户询问真实多人/真实噪声/交叠录音及人工标注路径，尚待提供。持久阶段故障历史已实现但待生产验收；原文忠实度候选及剩余纪要语义仍待改进，未因局部成果自动关闭。
