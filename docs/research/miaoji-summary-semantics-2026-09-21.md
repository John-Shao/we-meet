# 纪要占位责任人与条件语义复核（2026-09-21）

独立专项，不占共享批次号。Helm 392 backend/agents 为 `2651b6afa`，与持久诊断实现 `4ee18439e` 运行代码一致。

## 持久诊断生产结果

已用演示所有者账号新建两个固定合成样本，各只转写一次。结果保存在[Helm 392 证据](evaluations/miaoji-asr-production-392.json)：

| 输入 | 新任务 | 生产 API 结果 |
|---|---|---|
| 21.89 秒中文 | `7e1268d7-2dac-4568-baf4-9ef0dc89b5e0` | succeeded，3/3 输入确认，4 段原文，diagnostics 空。成功结论来自任务和正式原文，不来自空诊断。 |
| 3 秒静音 | `e09a3db6-ef4d-4a37-8287-f9f8fce91e0b` | incomplete / no_speech_detected，1/1 输入确认，0 原文；持久字段含 transcription_poll / no_speech / 6458ms。 |

第二账号 13800000008 读取两条 capture 状态均被拒绝；随后独立登录所有者重新读取，诊断与终态不变，仍各只有一项任务。未共享、重试、删除或修改旧任务。该路径由后端从数据库字段读取；未人为重启生产 Pod、未实测 30 天等待或破坏存储。新 Pod 复读及运维探针输出仍可在后续自然部署时补充；取消/过期/其他阶段继续以本地故障注入与数据库测试为证据。

## 本轮实现

1. `summary_facts.normalize_owner` 在引用验证后处理明确占位责任人：Unknown speaker、Speaker 2、未知说话人、TBD 等留空；覆盖本次实测新增的 Unknown speaker (I)、Speaker (I) 及中文括号变体。不根据“我”猜测身份，不创建业务待办。真实姓名、明确角色、组合姓名保留；若引用段落具有真实身份且显示名恰好类似标签，也保留。
2. 共用纪要提示明确条件/假设/实际事件的区别，要求概述也遵守；在相关要点保留否定金额/日期及 before 等时间边界。不能将提示当事实正确的保证。
3. 分块提示版本升级到 2，阻止新语义复用旧提取缓存。旧长文计划若以版本 1 在新 worker 执行，将在模型调用前失败为 invalid_output，保留旧纪要，需显式重新生成；不会自动付费重跑。部署前应让在途长文纪要完成。现有短文计划不受分块版本检查影响。

## 冻结模型对照与离线复验

输入为前一轮 6 个冻结快照/文本控制样本，加“条件但未失败”“实际失败及条件性通知”两个中文控制样本。[输入](evaluations/miaoji-summary-semantics-inputs.json)、[8 次首次模型输出](evaluations/miaoji-summary-semantics-candidate.json)均保留，模型 `qwen3.8-flash`，无供应商自动重试。

首次候选中模型仍输出 Unknown speaker (I) 和 Speaker (I)。扩充占位匹配后对**相同 raw_output 离线重做解析和引用校验**，结果保存在[离线复验](evaluations/miaoji-summary-semantics-normalized.json)，绑定原始输出文件哈希。没有再请求模型、覆盖首次失败或把离线处理伪称生产结果。

| 项目 | 观察与判定 |
|---|---|
| 占位责任人 | 离线最终结果两份英文 review 的 owner_text 均空；Alice/Bob/陈晨保留，relative due 不改写。确定性归一化不代表所有自然语言责任人均已识别。 |
| 否定金额/日期 | 英文两例保留 15,000 not 50,000；日期控制保留 Monday not Wednesday，没有虚构旧决定。 |
| 明确历史调整 | 明确批准 50,000 后改成 15,000 的控制样本仍保留真实变更，未被否定规则抹掉。 |
| 明确条件但未失败 | 保留当前上传成功、没有失败、重新上传未决定，action_items 为空。**输出为英文，未通过中文原语言保持。** |
| 实际失败 | 保留一次已发生的失败；陈晨、明天上午十点前重传；再次失败才通知李敏的条件仍在，未变成无条件通知待办。 |
| 中文含混原句 | ASR 版概述较保守；校对版仍写“指出临时文件清理失败的问题”，存在条件原则事件化风险。此项未通过，不用明确条件控制样本替代。 |
| 原文术语 | 未校对输入继续传播“庙记”，校对输入使用“妙记”。没有在纪要层静默修改原始转写。 |
| 引用与章节 | 8 例通过实际解析器的精确引用校验；多例 chapters 为空，不能据此宣称章节质量已验收。 |

评审者 Codex，尚未经人工复核。合成音频/文本不代表真人多人、噪声或交叠会议准确率。引用有效不证明每句语义受原文支持。

## 验证与下一步

53 项 summary versions/chunks/capture summary/评测器/初版归一化回归通过；新增 3 个占位变体后，归一化共 17 项通过，总独立相关用例为 56（重复运行不累加）。包括外部引用继续拒绝、原 revision/时间引用不改写、真实身份标签保留、旧分块版本不会付费执行。Ruff 与格式检查通过。

仅 backend 需要部署；不新增迁移、开关、权限或 API 字段。发布后对同一英文测试原文显式生成一次，验证占位字段为空、引用/历史版本保留。预算问题已在 Helm 391 定向通过；条件含混和原语言问题继续保留，不宣称“质量与故障诊断”整体完成。

真人轮流发言/真实噪声/交叠样本与人工标注仍待用户提供；App 画面受此前深链自动审批 blocked by policy 阻塞，未绕过。ASR 原语言及术语候选、其他冻结噪声样本的纪要事实基线也尚未全部覆盖。
