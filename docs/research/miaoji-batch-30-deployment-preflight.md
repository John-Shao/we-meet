# 第三十批：联合生产验收预检（部署阻塞）

2026-09-20，第 28/29 批完成后自动进入第 26–29 批联合验收，先验证线上接口可用性。

## 只读证据

生产 `https://meet.we-meet.online`：

| 无登录 GET | 结果 | 判断 |
|---|---|---|
| `/api/v1.0/meeting-records/` | 401 | 现有记录路由可达，要求鉴权 |
| `/api/v1.0/recording-hotwords/` | 404 | 第 27 批新接口当前不可用 |

当前代码的个人热词视图先执行身份检查，再检查功能开关；无登录请求应先得到鉴权响应。现有响应不能用于确认新后端已部署，也不能从这个探测反推确切生产 commit。此次没有登录、修改词表、创建记录、修改原文或新增分享。

## 阻塞与后续

- 联合生产验收等待包含第 26–29 批的 backend/frontend 发布。可执行现有发布流程：`bash deploy/aliyun/release-meet.sh backend frontend`。
- 发布流程须应用第 26/27 批的 `0183`、`0184` 迁移；第 28/29 批无新迁移。summary/agents 无本轮运行代码变更。
- Web/后端运行代码最新提交 `95a06f25d`，Android `a2a9a5b0`。新版 debug APK 已安装 Pixel 8，正式分发另行进行。
- 部署后依次验收批量校对、个人热词、发言分布及导出历史，继续使用用户授权的演示账号和测试记录；临时分享结束后撤销。
- 大文件恢复和长录音跨页生产验收继续延后。本批只有预检证据，不能标为生产验收通过；G4 组合筛选、G7 回收站等未实施项继续保留在真实缺口清单中。
