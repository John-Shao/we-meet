# 第三十批：部署预检与 App 联合生产验收

## 当前结论

用户于 2026-09-20 17:37:46 部署 backend/frontend `ad06f3e8a`（Helm revision 377），summary/agents 仍为 `f2e7f3cd4`。部署阻塞已解除；随后使用 Pixel 8 / Android 16、Android `a2a9a5b0` debug APK 和生产 API 完成以下验证。没有由本轮执行生产部署。

| 批次 | 生产证据 | 边界 |
|---|---|---|
| 26 批量校对 | App 预览不写入、确认替换、历史撤回通过；API 同 key 重放返回同一收据，撤回后重放不再应用；过期预览 409、撤回不能覆盖后续编辑；有效文本搜索更新，旧纪要内容未改 | 本轮未重做批量修改后的 TXT/SRT/VTT 系统保存、真实网络丢响应；不能代替此前本地回归之外的全部边界验收 |
| 27 个人热词 | 两账号词表隔离；App 旧版本保存 409 且保留草稿，放弃重载可保存；保存不改上传配置，点击合并才填入关键词；App 上传短音频后 ASR succeeded，提交期间清空个人词表，旧保存不能恢复清空内容 | 公开上传状态不返回冻结配置，本轮未观察供应商收到的具体热词，也未刻意触发上传任务重试；冻结/重试配置仍以本地测试为证据，不宣称识别质量达标 |
| 28 发言分布 | 视频展示 8 个发言区间、识别范围 0:00–0:47；点击 0:06–0:12 后播放器从 0:06 实际播放；08 App 可读分布且明确无播放权限 | 单一短样本，复杂重叠、多时钟、不完整时间和超预算降级仍主要由本地回归覆盖 |
| 29 导出历史 | 专用测试音频创建 6 个人工版本、每版中英文各一份，共 12 份私有文档；API 10+2 分页、无重复、第一页复读稳定；错误记录/篡改/跨账号游标均 400；App 下一页出现两份旧文档和上一页按钮，打开旧文档实际显示人工版本 1 的正文 | App 上一页按钮已显示，但本轮未点击回翻；本地已有前后翻页测试。指定 AI 版本导航沿用第 25 批证据，本轮旧文档验证为人工版本副本 |
| 跨账号及撤权 | 08 API 看不到 09 的导出回执，App 信息页为空；原文页面无批改/播放，直接批改/撤回请求 403；撤销临时纪要/全文授权后 API 404，App 清空记录内容并显示不可用 | 分享记录不等同于分享独立 Docs；本轮未单独请求他人 Docs 的底层接口 |

### 测试数据与恢复

- 批量校对使用已有验收视频 `edefb542-e50a-4851-964e-2682961939be`；所有临时文字已追加修订恢复原值，审计收据保留，旧纪要内容与测试前一致。
- App 使用 `test-audio01.m4a` 新增记录 `61307ff3-8aa2-47fe-ab9c-e4e4111947fd`，标题 `miaoji-b30-20260920-audio`。生成一份 AI 纪要后基于它创建 6 个人工验收版本和 12 份私有 Docs，作为分页验收样本保留；没有为了凑历史重复调用模型。
- 09 词表从空值恢复为空（revision 4），08 原空词表未改；两个记录的临时分享均撤销，已再次读取确认。用户其他记录未修改。
- 新增表上的读写成功，说明 0183/0184 所支持的生产路径可用；没有读取生产迁移日志，不将此写成迁移命令输出证明。

### 后续

本轮发现只读用户的空文档列表仍提示“可在纪要版本中创建”，与其权限不符，进入第 31 批修正。Web 实际页面、物理设备、Android 正式分发及上表限制继续保留；大文件恢复和长录音跨页按用户要求延后。G4 组合筛选、G6 可靠媒体时长/人工历史精确导航、G7 回收站等仍是真实开发缺口。

## 历史预检（部署前，已解除）

2026-09-20，第 28/29 批完成后自动进入第 26–29 批联合验收，先验证线上接口可用性。

## 只读证据

生产 `https://meet.we-meet.online`：

| 无登录 GET | 结果 | 判断 |
|---|---|---|
| `/api/v1.0/meeting-records/` | 401 | 现有记录路由可达，要求鉴权 |
| `/api/v1.0/recording-hotwords/` | 404 | 第 27 批新接口当前不可用 |

当前代码的个人热词视图先执行身份检查，再检查功能开关；无登录请求应先得到鉴权响应。现有响应不能用于确认新后端已部署，也不能从这个探测反推确切生产 commit。此次没有登录、修改词表、创建记录、修改原文或新增分享。

## 阻塞与后续

- 联合生产验收等待包含第 26–29 批的 backend/frontend 发布。可执行现有发布流程：`bash deploy/aliyun/release-meet.sh backend frontend`。
- 发布流程须应用第 26/27 批的 `0183`、`0184` 迁移；第 28/29 批无新迁移。summary/agents 无本轮运行代码变更。
- Web/后端运行代码最新提交 `95a06f25d`，Android `a2a9a5b0`。新版 debug APK 已安装 Pixel 8，正式分发另行进行。
- 部署后依次验收批量校对、个人热词、发言分布及导出历史，继续使用用户授权的演示账号和测试记录；临时分享结束后撤销。
- 大文件恢复和长录音跨页生产验收继续延后。本批只有预检证据，不能标为生产验收通过；G4 组合筛选、G7 回收站等未实施项继续保留在真实缺口清单中。
