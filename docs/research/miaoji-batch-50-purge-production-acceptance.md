# 第五十批：永久删除生产验收（原生完成，上传等待窗口中）

> 后续状态：[第五十三批](miaoji-batch-53-purge-final-acceptance.md) 已记录 Helm 384 后转写恢复、上传到期删除及非空历史删除的所列生产验收通过。本文保留提交时状态，未覆盖边界以第 53 批为准。

## 部署与范围

2026-09-21 用户确认 Helm **383**：启用配置 `ea5e5f25e`，backend `d2c791e9d`、frontend `62fb7904b`。启用后预检再次完整 PASS：API、worker、beat 均 `purge_enabled=true`，迁移、实例摘要、任务/队列、实际调度及四个存储探针通过，起始删除意图为 0。

本批使用演示所有者 `13800000009`、读者 `13800000008`，只删除本批新建且标题标有 `disposable` 的两个测试记录。Pixel 8 / Android 16 重新安装包含 `5e3c3cbb` 修复的 debug APK，通过 ADB / UI Automator 操作生产 App；没有正式分发或物理设备验收。

## 原生录音：已完成

- 记录 `e6d797f3-3751-4cb0-9782-89138dfd3641`，标题 `miaoji-b50-disposable-native`；capture `cfbeaaa6-65fb-48cf-b043-e8fe0684f652`。新建 media 保留模式，上传并封存一个 1 秒、16kHz 单声道 PCM WAV，正常停止。未生成 ASR/AI/人工纪要，不能据此声称非空纪要级联已做生产验收。
- 正常库状态请求永久删除返回 409；临时授权全文读者后，读者能读原文但请求删除返回 404。回收后，旧版本请求返回 409，读者删除与旧 capture 读取返回 404。
- App 回收站显示新入口，确认框展示本记录标题、删除范围及不可撤销提示。未勾选时确认按钮的可点击父节点 `enabled=false`；点击关闭后 API `GET purge/` 为 404，确认未创建删除意图。
- 再次打开并勾选后按钮可用，App 提交并显示后台处理中。收据 `expected_revision=1`，北京时间 **09:53:25** 受理，**09:53:29** 完成；App 实际显示 `Permanently deleted.`，关闭后该记录从回收站消失。
- 完成收据仍仅所有者可读；读者 GET/POST 均 404。相同原意图 POST 返回相同完成收据，错误版本返回 409。恢复返回 404。
- 原记录、原文、AI 版本、人工纪要、导出历史、媒体及 capture/音频分片/转写旧接口全部 404。通过源站凭据独立 HEAD 对应 WAV 对象也为 **404**，不是仅根据 App 提示推断媒体消失。

## 上传音频：受理/等待保护已通过，最终完成待复核

- 新记录 `010ed377-eb54-4532-aa3d-85afb8322775`，标题 `miaoji-b50-disposable-upload`，来自用户提供的 `test-audio01.m4a`，使用独立的新上传幂等键。
- 转写完成且有 **18 段原文**后才回收并受理永久删除，收据版本为 1。
- 生产直传 TTL 为 **3600 秒**；服务端 `not_before=2026-09-21T02:49:50.748421+00:00`，即北京时间 **2026-09-21 10:49:50**。未改动 TTL、时钟或数据库任务时间。
- 等待中状态为 pending；恢复返回 409，相同意图重试返回同一收据，错误版本 409，其他账号读取/请求删除 404。
- App 回收站只保留删除状态入口，实际显示后台处理中、不可恢复、最早开始清理时刻及可关闭窗口提示。模拟器使用 UTC，界面显示 02:49，与上述北京时间表示的是同一时刻。
- **尚未验收**：到期后的 complete、旧上传媒体/18 段原文不可读、完成收据幂等性和旧上传幂等键无法复建。已在本地保存精确记录与收据及到期复核脚本，后续沿用这个样本，不重新创建或提前宣称成功。

## 保留与边界

既有回归记录 `9a07360f-3a39-420a-abe8-7ca0b87a229a`、`61307ff3-8aa2-47fe-ab9c-e4e4111947fd`、`edefb542-e50a-4851-964e-2682961939be` 本轮仅读取，仍可访问、未回收。原生样本的临时分享随回收撤销，源记录已完成删除；上传样本保持明确受理的不可恢复状态，交由后台到期执行。

本批没有应用代码或配置变更，无需再次部署。证据为生产 API、App UI、源站 HEAD 和用户回传的运行预检；不是主库逐表独立审计、备份/外部副本擦除证明。非空 AI/人工历史生产级联、旧直传 PUT/完成凭证重放、失败重试/故障注入、Web 实际交互、正式 App 分发仍未在本批覆盖。自动保留期、大文件恢复、长原文跨页继续延后。
