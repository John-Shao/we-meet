# 第二十一批：生产签名自然到期与 Android 恢复验收

2026-09-20，北京时间 11:23–12:28。生产 backend `2eb3482c6`；Pixel 8 / Android 16。Android 运行修复为 `a06355a6`，生产验收断言提交 `b80d5e21`，均已推送。本批未修改后端、生产时钟、对象存储权限或 3600 秒签名有效期。

## 自然过期：真实 API 与对象存储

使用本次验收创建的视频记录 `f5387755-a083-47d7-9c60-5e9065203d9a`，对应用户提供的 `test-video01.mp4`，19,855,680 字节。

| 检查 | 实际结果 |
|---|---|
| 旧 URL 的签发时间 | 2026-09-20 03:23:45 UTC，即北京时间 11:23:45 |
| 后端及签名参数有效期 | 3600 秒 |
| 到期前请求 | 后段 Range 返回 206、1024 字节 |
| 到期后自动探针 | 04:23:50.935 UTC：旧 URL 返回 403 / `AccessDenied`；存储服务 Date 为 04:23:50 GMT |
| 再次确认 | 04:24:22.063 UTC：旧 URL 仍 403；不是修改 URL 或伪造过期参数 |
| 所有者重新获取签名 | API 200，URL 已改变，`expires_in=3600` |
| 新 URL 的后段请求 | 206；`Content-Range: bytes 19000000-19001023/19855680` |
| 返回内容 | 1024 字节，与本地原文件相同偏移处逐字节一致 |

## Android 真实界面与原生解码

两部分证据分别记录，避免把组件验收混同为完整 App 连续播放一小时：

1. **生产 App 前台暂停恢复**：打开 `miaoji-app-20260920-video`，在 14 秒、1.5 倍速暂停。北京时间约 11:58 至 12:24，持续约 26 分钟，多次观察位置和倍速未重置、未自行播放。随后点击播放，从 14 秒继续到 23 秒，再次暂停；竖屏画面正常，1.5 倍速保留。
2. **真实生产过期 URL + 原生组件**：显式运行 `ProductionMediaRenewalAcceptanceTest`。把已经自然失效的 URL 交给实际 `UploadMediaPlayer` / `MediaPlayer`，观察可重试错误；通过同一所有者生产 API 获取新 URL 后重试。恢复后的首个位置在保留的 4 秒附近，并非从零播放；1.5 倍速保留；定位到 40 秒后实际解码时钟继续增长，再暂停正常。1 项生产仪器测试通过，0 失败、0 跳过，测试方法耗时 43.328 秒。

生产仪器测试使用第二十批默认 30 秒准备上限，没有换成回归用的 1.5 秒期限。自动换新链接、持续失败的重试上限，以及暂停后换链的组合分支，由第二十批 13 项播放器仪器测试覆盖。23 项详情页仪器测试见第十九批。

首次生产仪器运行被临时 loopback 通道残留的“到期前”状态阻止，未进入媒体阶段；更新通道状态后上述完整测试通过。该问题属于验收通道，不是生产 API 或 App 的新故障。签名未写入源码、命令行或本报告；临时服务和 ADB 端口转发已关闭。

## 结论与边界

- 本次优先要求的两项验收已完成：签名自然过期/续期见本批；跨账号纪要/全文权限与撤权见[第十九批](miaoji-batch-19-cross-account.md)。测试分享最终为空。
- 发现并修复两处 Android 问题：只读上传件误请求所有者转写状态；原生 HTTP 403 长时间停在准备态。对应 `88934269`、`a06355a6`，验收断言 `b80d5e21`。
- 修复版 debug APK 已安装到模拟器；正式 App 分发仍由发布流程完成。没有新的后端/Web 部署要求。
- 26 分钟暂停和真实过期链接组件测试不能等同于物理设备上连续解码一小时、系统杀进程后恢复或后台长时间播放。Web 页面和物理设备仍待后续验证。
- 按用户要求，大文件恢复与长录音跨页继续延后。
