# 第五十三批：转写恢复与永久删除生产验收收尾

## 部署基线

用户回传 2026-09-21 Helm **384**，agents `9ebc986af`；backend `d2c791e9d`、frontend `62fb7904b`、summary `f2e7f3cd4` 不变。本地比较 `8cada0f4f..9ebc986af` 的 `src/agents` 与 worker Helm 模板无差异，所发布提交包含第 52 批存储修复。实际集群版本依据用户回传，不冒充本地拥有生产 kubectl。

本批仅使用此前的两个可销毁样本，所有者 13800000009，权限验证读者 13800000008。没有新增运行代码或配置，无需再次部署。

## 上传样本：等待结束、重放防护通过

记录 `010ed377-eb54-4532-aa3d-85afb8322775`，此前确认有 18 段原文。

- 原始 not_before 为北京时间 **10:49:50.748**，后台完成时间为 **10:49:59.778**，没有缩短 3600 秒 TTL 或修改任务时钟。
- 原记录、原文、AI 版本、人工纪要、导出历史、媒体、旧上传任务端点均 404；恢复也为 404，回收站列表不再包含该记录。
- 所有者仍可读最小完成收据；相同原始版本的删除请求返回同一收据，错误版本为 409；第二账号读取及请求删除均为 404。
- 使用原文件、原文件名及原上传幂等键再次调用普通上传接口，返回 **409**，没有复建记录。
- Pixel 8 App 回到前台后实际显示 `Permanently deleted.`；关闭后回收站空态显示正常。
- 本次没有获取上传对象键并独立 HEAD，源上传对象的完成以 worker 收据为证，不将其混同下文三个原生分片的独立源站核验。旧预签名 PUT/直传完成凭证重放未在本次覆盖。

## 原生转写恢复：同一样本的第二代成功

记录 `c6e57fed-e82f-4992-a4e0-2ee93c3b6d35`，capture `009d0729-b622-47e7-9b10-09b08e775b05`，约 29.981 秒。

- 明确确认旧任务为 incomplete 后，仅发起一次新转写意图，任务 `5a53db59-3afe-4bc2-9ba0-cc7baba668ac`、generation 2，最终 **succeeded**，3 个输入均确认，产出 **6 段原文**。
- 删除前读取代次列表，确认第一代 `e889ad59-a1d3-4e5f-88f6-d56a94bc1685` 仍为 incomplete，没有覆盖旧失败或伪造成功。
- 实际生成 **1 个 AI 纪要版本**，再保存 **2 个人工历史版本**，从第 1 个人工版本导出 **1 份私有 Docs 副本**；逐个读取历史 UUID 成功，导出为 ready。没有注入内部原文或跳过供应商。
- 这证明第 52 批修复后的当前生产原生 sealed ASR 样本链路恢复，不代表所有语言、长音频、实时转写或质量指标已全量验证。

## 非空历史删除与外部文档边界

- 在删除前读取并保存独立 Docs 标题及 `formatted-content/?content_format=json` 返回正文的 SHA-256；第二账号直接读取该文档被拒绝。
- 将本记录回收后，实际通过 App 打开永久删除确认框、核对标题/范围、勾选不可恢复确认并提交。App 显示处理中；服务端北京时间 **10:53:44.797** 受理，**10:54:00.470** 完成。
- 完成后原记录、原文、AI 版本/任务、人工当前稿/历史列表及两个具体历史 UUID、导出列表、媒体、capture/音频列表/转写及三个具体分片端点均为 404。完成收据幂等、第二账号不可读/不可删除、不可恢复及回收站消失均通过。
- 独立使用源站凭据 HEAD 三个已知 `capture-audio/<record>/<capture>/<chunk>.wav`，全部 **404**。不是只根据 UI 判断源音频已消失。
- 外部文档 `7e217e82-dd8a-43a0-9880-9ccb9caee2cb` 所有者读取仍为 **200**，标题及正文哈希均与删除前一致，第二账号仍被拒绝。此私有测试副本按既定独立 Docs 生命周期保留，没有删除或共享给别人。
- 最后复核时模拟器切换到了另一应用的活动会话，UI Automator 返回 137。没有打断该会话或将其当作 we-meet 故障。本原生样本的 App 证据覆盖确认/提交/处理中，最终完成及回收站消失依据 API；不声明本样本 App 最终完成画面已看到。上传样本完成画面则已实际看到。

## 结论与保留边界

第 50 批上传到期复核、第 51 批原生转写阻塞、第 52 批修复的生产复测，以及非空原文/AI/人工历史/导出关联删除范围的本轮验收已闭环。主库逐表擦除、备份/已交付副本擦除不在本次证明范围；指定旧接口不可读不单独构成主库逐表审计。

旧回归记录 `9a07360f-3a39-420a-abe8-7ca0b87a229a`、`61307ff3-8aa2-47fe-ab9c-e4e4111947fd`、`edefb542-e50a-4851-964e-2682961939be` 仅读取，仍可访问且未回收。本原生样本删除前保留的失败/成功代次属于其记录内容，永久删除后不要求其业务任务继续存在；失败历史证据已记录于第 51–53 批。

仍未覆盖：旧预签名 PUT/直传完成重放、故障注入/失败重试、Web 实际删除交互、正式 App 分发/物理设备，以及本原生样本的 App 最终完成画面。自动保留期限/管理员治理仍是后续开发范围；大文件恢复和长原文跨页继续按用户要求延后。不要把已完成的手动永久删除重新列为未实现，也不要把剩余场景都标为已通过。
