# 第三十六批：Android 排序和日期错误提示生产验收

2026-09-20，Pixel 8 / Android 16，Android `ce1fcead` debug APK，连接生产 backend/frontend `85c0291d6`（revision 378）。通过正常 App 短信登录流程使用 09 账号，全程 ADB/UIAutomator 操作实际页面。

## 已验证

- 记录库选择“我的内容”，标题 `miaoji`、来源“导入文件”、创建日期 2026-09-01 至 2026-09-30。默认从新到旧依次为 `miaoji-b30-20260920-audio`、`miaoji-app-20260920-video`。
- 选择从旧到新后两者反转，再切换从新到旧恢复原顺序；归属、日期、来源、搜索内容和结果集合保持不变。
- 将开始日期暂改为 2026-10-01（晚于 2026-09-30），点击完成后正确拒绝。实际截图显示面板保持完整展开，错误文字、清除日期和完成按钮均可见，键盘已关闭；关闭面板后仍保留原来的有效日期范围。
- 纪要库“我的”按 `miaoji` 搜索，两条既有验收记录可以从新到旧、从旧到新，再切回从新到旧；搜索内容和归属保持不变。

第 34 批面板问题、第 35 批两个列表的生产 App 排序入口已通过这些场景。没有上传、新增/修改记录、编辑纪要、导出 Docs 或新增分享；第 33 批临时授权保持撤销。

## 未覆盖

两条结果没有分页，不代替生产多页后切换排序；本轮没有进行中样本、日期与创建时间不同的线上会议样本，也没有切换设备时区或使用物理设备。纪要库本轮验证归属/标题与排序，日期/来源组合在记录库验证。模拟器 debug APK 不代表正式 Android 分发或 Web 页面验收。

无运行代码变更，无需部署。大文件恢复、长原文跨页按用户要求继续延后。
