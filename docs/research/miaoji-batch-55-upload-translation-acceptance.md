# 第 55 批：上传全文翻译生产验收

## 结论与部署基线

2026-09-21，用户部署回执确认 Helm **385**，backend / celery-backend / celery-beat / frontend 使用 **a616ec42e**；agents 保持 **9ebc986af**，summary 保持 **f2e7f3cd4**。生产翻译查询、创建及后台处理成功，说明所测接口及任务所需表可用；本轮没有直接读取集群迁移表，不将发布日志等同于逐 Pod 迁移审计。

Pixel 8 / Android 16 安装第 54 批 **48af303a** 对应 debug APK，通过普通登录、会议导航和记录库进入原生详情。最初恢复到旧记录弹窗、随后登录 WebView 加载；正常登录后功能可用，未复现持续白屏或崩溃。不是正式 App 分发或物理设备验收。

**本批所列生成、对照、回听、导出、修订失效、跨账号访问和回收恢复通过；永久删除最终完成待保护窗口到期复核。** 没有运行代码/配置改动，无需再部署。

## 独立样本

- 仅新建并操作 `miaoji-b55-upload-translation`，来源为用户提供的 `test-audio01.m4a`。
- 记录：`55cbba86-4e3b-464d-afa2-a6607a7207d0`；上传/ASR attempt **1**，成功得到 **18 段**，播放器媒体时长显示 **1:33**。
- 所有者测试账号尾号 09，被分享测试账号尾号 08。分享仅作用于本记录，验收后已撤销全文与纪要授权。未发送聊天消息或通知。
- 原文、导出文件和认证凭据仅保存在本地临时验收目录，不提交到仓库；下文仅记录任务标识和结论。

## App 与真实供应商链路

| 项目 | 实测证据 |
|---|---|
| 中文原文 → 英文 | App 点击生成；任务 `f6ceaef7-b924-47d7-ad2d-4c177d2e79a3`，原文 revision 2，succeeded，1/1 批、18/18 段；详情与原文逐项比对 segment ID、原文、起止时间全部一致，末页为空。 |
| 阅读与回听 | 原生译文页显示原文/译文对照；第二段“Play this segment”从约 0:06 开始，随后播放位置正常推进，时长加载为 1:33。只验证该段及原始样本时钟，不声称人工英文控制文本与音频吻合。 |
| 语言切换 | 未生成中文时，切换显示空态与显式生成按钮；切回英文复用已有产物，没有因为切换自动创建任务。 |
| 三种导出 | App 经系统文件选择器分别保存 TXT/SRT/VTT；拉取保存文件，与对应生产 API 响应逐字节一致。全部包含 18 段译文；SRT/VTT 均为 18 个时间段，VTT 有正确文件头。响应带 no-store。 |
| 成功产物复用 | 对当前成功语言/修订使用另一请求键并重放，均返回原任务 ID；当时任务列表仍仅 1 项。证明业务产物复用，不单独证明供应商账单只有一次调用。 |
| 原文修改后失效 | 使用正常校对 API 将本测试记录首段改为标有 Acceptance control 的英文控制文本，revision 2 → 3。旧成功译文 stale=true；App 自动显示原文已变化，移除旧版导出/定位；旧 TXT/SRT/VTT 导出均 409。旧快照正文仍可作为明确标旧版本读取。 |
| 重新生成 | App 显式再次生成英文；新任务 `39c4af0b-6bed-482b-8049-5b12841e26ae`，revision 3，18 段 succeeded；新快照使用校对后的首段，旧任务保留且仍过期。 |
| 英文控制段 → 中文 | App 显式生成中文，任务 `771796f8-f904-4cef-a18a-456062ea1edf`，revision 3，18 段 succeeded。首段“不要重启流 / 等待10秒 / Core必须保持现有会话”保留否定、数字及术语。其余是中文原文，属于**混合语言校对文本**测试，不是完整英文音频 ASR 样本。 |
| 上一批上传重放修复 | 再次普通上传同一文件/文件名/原请求键，生产返回 202、同一 record ID、succeeded、attempt 1；未新建记录或重跑转写。 |

真实译文已人工核对全部 18 段对应关系及主要语义。口语重复被整理，原文中的“沃尔沃”译为 Volvo，但本轮没有校验该名称是否由 ASR 正确识别；第 11 段“重新配网”英文带有解释性措辞，不能作为严格逐字翻译或语义零误差的保证。ASR 质量与全文翻译完整性应分开评价。

## 跨账号与生命周期（生产 API 证据）

- 未分享：账号 08 访问列表、具体译文、导出均 404。
- 仅纪要分享：账号 08 可读记录，但译文列表/详情/导出仍 404。
- 全文分享：账号 08 可读全部 18 段及 SRT，导出字节与所有者一致；can_generate=false；尝试生成返回 403。
- 撤销全文和纪要权限后：账号 08 对已有列表、详情、导出 URL 均 404。没有残留测试授权。这里不宣称第二账号 App 前台撤权画面已实测。
- 三份成功译文存在时，回收后全部详情及导出拒绝；恢复后 **3 份完整详情与回收前逐项相同**，包括两种目标语言、18 段快照及旧版 stale 标识。
- 最后再次回收并受理本记录永久删除；状态 **pending**，expected_revision **3**，恢复请求 409，三个旧译文及导出 URL 404。
- 删除保护窗口：`not_before=2026-09-21T05:21:34.694344+00:00`，即北京时间 **2026-09-21 13:21:34.694**。本轮未缩短等待，也没有提前标记物理删除完成。到期仍需检查完成收据、译文旧入口与源文件实际清理；等待期间不可读不能代替最终删除证据。

## 保留边界及下一步

1. 北京时间 13:21:35 后复核这条专用记录最终删除，记录完成时间。若仍 pending/failed，则按实际收据继续诊断。
2. 本轮未直接核对生产数据库的逐表擦除和 AIUsageRecord，也未取得供应商请求/账单计数；“一次业务任务、1/1 批成功”不升级为供应商仅执行一次的独立审计结论。第 54 批已有重复 worker 本地测试。
3. 完整英文音频 → 中文、更多语言/术语质量基准、第二账号 App 撤权画面、Web 浏览器全流程、正式 APK 分发/物理设备仍未覆盖；不据此否定已完成的中英文实现和所列现网流程。
4. 用户延后的大文件恢复、长录音跨页继续延后；LiveKit Egress / ECS 条件仍未解决，线上统一录像不在本批。
