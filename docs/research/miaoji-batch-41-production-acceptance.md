# 第四十一批：部署后时长、历史来源与关闭回收开关验收

## 环境与范围

用户提供的部署日志确认 2026-09-20 22:03（Asia/Shanghai）Helm revision 379，backend/frontend 与 celery-backend 为 `bd16ac487`，backend、celery-backend、celery-beat、frontend rollout 均成功。summary/agents 保持原版本，符合第 40 批发布范围。

Pixel 8 Android 16 通过 ADB/UI Automator 操作生产 App。Android `9fcdb382` 重新构建并安装，资源文案修正已进入本次测试。模拟器显示 UTC，因此界面新样本创建时间为 14:13，对应北京时间 22:13。仅操作专用验收样本；API 登录凭据仅在临时进程内，未纳入仓库。

## 通过项

1. **已部署的数据契约与关闭开关**：详情返回 `lifecycle_revision: 0` 和 `media_timing`；列表 `trash_available: false`，所有者 `capabilities.trash: false`。回收列表和生命周期写入均 404，记录仍可正常读取，未授权账号读取 404。App 库入口及信息页不出现回收操作。查询成功意味着新列已经可用；没有直接读取生产 `django_migrations` 表，不把 rollout 日志当作迁移表证据。
2. **旧音频播放器时长**：`61307ff3-8aa2-47fe-ab9c-e4e4111947fd` 的服务端时长仍为 unknown。App 准备播放器后，信息页显示 `1:33`，与播放器一致。
3. **视频与记录切换**：从上述音频切换到 `edefb542-e50a-4851-964e-2682961939be` 后，未准备时播放器归零；准备后播放器及信息页均显示 `0:47`，没有继承音频的 `1:33`。服务端仍将旧视频时长标记 unknown；实际画面、信息页截图已检查。
4. **新音频供应商时长**：App 从系统文件选择器导入已有测试音频 `test-audio01 (1).m4a`（2,322,932 字节），上传并转写成功。新增专用样本 `9a07360f-3a39-420a-abe8-7ca0b87a229a`，命名 `miaoji-b41-20260920-audio`。服务端返回 `basis: original_audio`、`duration_ms: 93461`；识别区间最末端为 91970ms，未被误用为媒体总长度。尚未播放时 App 信息页已显示 `1:33`，发言页标尺为 `0:00–1:33`，18 个发言区间、识别时长 `1:23`，分母提示仍为识别发言时长。记录生命周期版本保持 0。
5. **人工来源不是当前稿替代品**：旧音频当前人工版本为 6，点击较早导出的“查看来源纪要版本”实际打开只读版本 5，并保留 `history copy 5` 验收标记。接口核对来源 UUID `9907e356-e760-46c6-9c0c-cebc1473f938`、正文与 input snapshot；正文不同于版本 6。不存在的历史 UUID 返回 404，不回退到当前稿。截图已检查，修正后的中性来源文案可见。
6. **仅纪要分享及撤权**：只对上述专用旧音频临时授予测试读者纪要访问。读者可以读取相同历史版本，但无原文/播放能力；原文接口 403，历史写入 405。撤权后历史读取 404；所有者的版本 5、当前版本 6 和首屏导出集合均未改变。临时授权已在 finally 中撤回，未创建任何导出、通知或人工修订。

## 边界与下一批

- 本批是 App UI 加生产 API 验收，不代替 Web 页面、正式 APK 分发、物理设备或全部异常条件验收。
- 完整/部分原生采集、历史页撤权时的 App 可见内容清理仍只有此前回归证据，本批未构造这些生产样本；已有同一快照的版本 5/6 不证明跨不同快照的全部引用行为。
- 没有回收或删除生产记录。回收开关继续关闭；下一批准备启用配置，用户发布 backend 后再以本批新样本核验回收、恢复、分享不复活、旧入口拒绝及任务忙态。
- 永久清理尚未交付；大文件恢复和长原文跨页继续按用户要求延后。
