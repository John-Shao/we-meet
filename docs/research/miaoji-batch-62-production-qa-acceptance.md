# 第 62 批：短语与回答约束生产复测

日期：2026-09-21。用户回传 Helm **388**，backend/celery-backend/beat **4c5ca713f**，frontend **a616ec42e**、agents **9ebc986af**、summary **f2e7f3cd4**。部署版本依据用户日志，本轮没有登录集群核对镜像。

## 结果

第61批两个具体问题在本次生产样本中改善：明确短语问题的旧会议候选 **8→0**；自然问题回答不再添加“严格控制成本”等无原句依据的解释。原自然问题、明确短语、随机空查询和未来日期空态均通过所列 API 检查。服务端非空 SSE 请求正常完成。

**App 本轮 UI 验收待补**：Pixel8（emulator-5554）已有其他 UI 自动化会话占用，`uiautomator dump` 报 `UiAutomationService ... already registered`；启动 we-meet 后前台再次切换到另一个活动中的联调 App。停止输入，未终止对方会话、未重启模拟器，也未以第60批截图冒充本轮结果。已询问用户等待占用结束还是换设备。

本批只有脱敏验收报告与文档，无运行代码、镜像或再次部署要求。第60批 Android `5f330902` 保持，正式分发仍待办。

## 同题 API 对照

使用既有演示账号09与 `scope=meetings`，重放第58/60批的相同问题，没有创建或更改记录、分享。生产报告见 [miaoji-qa-production-b62.json](evaluations/miaoji-qa-production-b62.json)。每题仅一次请求，不以重复生成挑选答案。

| 场景 | 本轮结果 | 客户端耗时 |
|---|---|---|
| 两种老板思维中，资本家与奴隶主最大的区别是什么？ | 8条统一记录，0条旧来源；回答“帮他赚钱/挣他的钱”引用[5]，无额外成本解释。 | 13.502秒 |
| “两者之间最大的区别”是什么？ | 8条统一记录，0条旧来源；核心区别引用[1][6][7][8]，均有原句支持。 | 4.824秒 |
| 与第58批相同的不存在标记 | 空引用、无模型、罐头回答。 | 2.514秒 |
| 资本家，2099-01-01至2099-01-02 | 空引用、无模型、罐头回答。 | 4.356秒 |

两题非空返回实际模型 `qwen3.8-flash`、degraded=false。自然问题比上次12.415秒略慢，明确短语比上次6.584秒短；均为单次客户端端到端耗时，不能推断SQL、P95或整体性能变化。

8条不同记录逐一读取成功；自然问题1条、短语问题4条实际使用引用，均与当前有效原文及 **34740ms** 的start_ms精确对应。未重复跨账号撤权/流式中途撤权循环，不扩展为全部权限验收通过。

## 逐事实复核与 SSE

Codex助手核对本轮自然答案：前言明确“根据资料”；两种视角对应原文“资本家觉得你是在帮他赚钱，奴隶主觉得你是在挣他的钱”。没有新增管理政策、成本控制或动机断言。短语答案同样有支持；多处相同出处不等于更多独立证据。这是本次回答的逐事实核对，不是独立人工盲评或线上准确率。

通过正式 `/search/ask-stream/` 端点，`Accept: text/event-stream` 重问同一短语：HTTP200，正确Content-Type，1个meta、11个delta、1个done，degraded=false，客户端3.692秒。拼接回答与非流式短语结果一致；已用引用的record/start/snippet与上面已核验来源一致。没有406，但这只验证服务端流式链路，不能替代尚待补齐的App本轮展示和点击导航。

## 仍未关闭

- 重复上传/重复出处仍能补位成多条相同引用；本轮短语仍8条统一记录，不能宣称跨记录语义去重完成。
- 原31题四道同义/跨语言漏召回（21/25）、无引号向量相关性、全来源共同预算、独立留出集和大库性能仍待评测。
- 第61批固定证据冲突题的来源日期未完整复述；本轮没有用真实冲突记录进一步验证。
- App本轮端到端、正式分发、物理设备与Web UI仍按各自边界待办。大文件恢复、长录音跨页和LiveKit Egress/ECS继续后置。

下一步先在可独占的设备补App回答及引用跳转；检索质量后续工作保留原冻结题库，不通过修改gold或扩候选数掩盖语义题失败。
