import { css } from '@/styled-system/css'

export const libraryLayout = css({
  maxWidth: '960px',
  width: '100%',
  margin: '0 auto',
  padding: '1.5rem',
  overflowY: 'auto',
  overflowWrap: 'anywhere',
})
