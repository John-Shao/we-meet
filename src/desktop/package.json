{
  "name": "we-meet-desktop",
  "private": true,
  "version": "0.2.0-d0.8",
  "description": "we-meet 桌面端 (Electron) — 复用 src/frontend 的 React 应用",
  "main": "dist/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:main": "tsc",
    "build:renderer": "node scripts/build-renderer.mjs",
    "copy:renderer": "node scripts/copy-renderer.mjs",
    "dev": "npm run build:main && electron .",
    "test": "npm run build:main && node --test tests/*.test.cjs",
    "test:smoke": "npm run build:main && node scripts/smoke.cjs",
    "package": "node scripts/verify-electron.mjs && npm run build:main && npm run build:renderer && npm run copy:renderer && electron-builder --win --x64 && node scripts/release-manifest.mjs",
    "package:prepared": "node scripts/verify-electron.mjs && npm run build:main && electron-builder --win --x64 && node scripts/release-manifest.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "44.4.3",
    "electron-builder": "26.15.3",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "jose": "6.2.2"
  }
}
